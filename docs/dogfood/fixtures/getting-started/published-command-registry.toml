# Offline published first-run command registry (#2353).
# Source: git tag v0.1.11 `crates/cargo-allow/src/cli.rs` `SUBCOMMANDS`.
# Ordinary PR checks use this snapshot (no crates.io download).
# Installed-binary execution of snippets remains #2278.
# Update this file atomically when promoting the next published release.

schema_id = "cargo-allow.published-quick-start.v1"
version = "0.1.11"
channel = "published"
source = "git-tag:v0.1.11:crates/cargo-allow/src/cli.rs:SUBCOMMANDS"

# Full published binary subcommand surface (exact tag snapshot).
subcommands = [
  "init",
  "audit",
  "check",
  "diff",
  "list",
  "explain",
  "why",
  "add",
  "propose",
  "worklist",
  "migrate",
  "refresh",
  "prune",
  "doctor",
]

# Subcommands taught as ordinary Published first-run / quick-start instructions.
# Must be a subset of `subcommands`.
first_run_subcommands = [
  "doctor",
  "audit",
  "init",
  "propose",
  "check",
  "list",
  "explain",
  "why",
  "worklist",
  "diff",
  "add",
  "refresh",
  "prune",
]

# Current source-candidate first-run surfaces that were not present at the
# 0.1.11 freeze. They must remain explicitly labeled in getting-started.md.
candidate_only_subcommands = [
  "capabilities",
]

# Root/shared flags taught on the Published path (docs may use these).
# Not a full clap inventory; only the first-run contract surface.
first_run_flags = [
  "--locked",
  "--version",
  "--root",
  "--write",
  "--mode",
  "--format",
  "--receipt",
  "--output",
  "--base",
  "--kind",
  "--dry-run",
  "--config",
  "--path",
  "--line",
  "--allow-id",
  "--profile",
  "--repo-policy",
]
