# Seven-crate IntentCandidate package / install set (#2599-C, #3147).
# Shared by intent-candidate smoke and extraction parity receipts.
# Do not fork this list into additional independent copies without updating callers.

schema_id = "cargo-allow.intent-candidate-crate-set.v1"
schema_version = 1

# Dependency / package order (leaf → binary).
crates = [
  "effortless-repo-protocol",
  "effortless-repo-snapshot",
  "effortless-rust-source-index",
  "intent-model",
  "intent-protocol",
  "intent-compiler",
  "cargo-intent",
]
