# Four-crate ProofCandidate package / install set (#2589-B, #2937).
# Shared by proof-candidate smoke and extraction parity receipts.
# proof-provider-api and proof-adapter-command were absorbed into proof-orchestrator.

schema_id = "cargo-allow.proof-candidate-crate-set.v1"
schema_version = 1

# Dependency / package order (leaf → binary).
crates = [
  "effortless-repo-protocol",
  "proof-protocol",
  "proof-orchestrator",
  "cargo-proof",
]
