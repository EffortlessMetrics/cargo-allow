# 0.1.4 Release Candidate

This is a release-candidate record for a future `0.1.4` patch release. It
records the post-0.1.3 scope currently merged on `main`. This is not a
completed release record. It is not a publication record. `0.1.4` has not been
tagged or published.

The workspace package version and internal dependency requirements have been
staged at `0.1.4` for package dry-runs. Public install examples remain pinned
to the latest verified crates.io release until publication is observed.

If this release is published later, replace this candidate note with the final
observed publish, registry, installed-binary, and rollback evidence.

## Target Claim

The 0.1.4 release keeps the 0.1.x claim:

```text
cargo-allow inventories syntax-visible source exceptions and checks them
against a policy ledger.
```

It may additionally say that setup diagnostics, evidence-health repair queues,
stale-prune previews, and source-tree initialization flows are easier to route
from saved artifacts and human output.

It must not claim macro-expanded, type-aware, MIR-level, build-aware,
control-flow, data-flow, unsafe-proof, test-adequacy, or coverage-proof
analysis.

## Candidate Scope

0.1.4 is a patch candidate for product hardening merged after 0.1.3:

- Crate-level docs now keep the source-tree/no-code-execution claim boundary
  visible in published library packages.
- Package metadata now points each crate at its crate-local README, while the
  root README remains the product landing page.
- Human `explain` and worklist output make evidence diagnostics easier to scan
  and route without parsing JSON.
- Stale-prune output includes TOML removal previews, and stale worklist proof
  commands route through dry-run and JSON prune artifacts.
- Worklist proof commands now include ledger lookup shortcuts such as
  `list --allow-id`, durable allow-ID queues, and common shortcut queues for
  baseline debt, broad scope, broken evidence, and weak evidence.
- Migration summaries now expose more adoption-health context: evidence-bearing
  entry counts, lint-exception entry counts, and weak-evidence reference counts
  when legacy conversion preserves unstructured evidence that still needs
  review.
- Repo-policy migration coverage now includes legacy Clippy exception and
  unsafe allowlist inputs in the combined migration path.
- Weak-evidence work items for unsafe exceptions now route humans and agents
  toward unsafe-specific evidence such as unsafe-review, boundary tests, specs,
  or equivalent review receipts.
- Doctor, audit, check, diff, HTML, and SARIF reports surface evidence-health
  repair queues for broken local evidence links and weak evidence references.
- Policy validation rejects padded reason text so reviewable rationale does not
  hide whitespace-only edits.
- The Rust source-syntax scanner now detects source-visible `cfg_attr`
  conditionals that apply `unsafe(...)`, `allow(...)`, or `expect(...)`
  attributes, while ignoring quoted documentation strings and custom attribute
  names such as `my_allow(...)`.
- Post-candidate scanner hardening now also records the source-visible identity
  needed to distinguish more findings structurally: unsafe attribute columns,
  multiple `cfg_attr` unsafe/lint attributes, spaced attribute tokens,
  multi-lint attributes, multiple index expressions on one line, index receiver
  identity, panic macro path identity, unsafe function symbols, and unsafe trait
  symbols.
- `cargo-allow init --root <source-tree>` resolves relative config output from
  the diagnosed source-tree root.
- `cargo-allow doctor` suggests the root-aware standalone init command in human
  output and saved JSON when no policy config is found.

Non-goals for 0.1.4:

- No new scanner, matcher, migration, or policy broadening to make checks pass.
- No claim of build-aware, Cargo-metadata, Clippy, rustc, proc-macro, or
  proof-tool execution.
- No laundering generated `baseline_debt` into reviewed approval.
- No compatibility claim for external dogfood lanes that were not revalidated
  during this release turn.
- No public install example update until 0.1.4 registry visibility and
  installed-binary verification are observed.

## Candidate Evidence

Candidate checks observed for this PR:

```text
rtk cargo fmt --all --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
rtk cargo package --list --workspace
rtk cargo publish --dry-run -p allow-core --locked
```

Observed no-new posture after staging the candidate files:

```text
62 matched, 0 new, 0 stale
543 source-tree inventory files scanned via git_tracked
```

Observed source-exception inventory from the check receipt:

```text
lint_exception: 1 matched
non_rust_file: 56 matched
panic: 3 matched
unsafe: 2 matched
```

Earlier pre-publication preflight was rerun from a clean `main` tree after the
clean package-evidence update merged:

```text
rtk cargo fmt --all --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
rtk cargo package --list --workspace
```

The full workspace test run passed with `882` tests across `33` suites, and the
refreshed no-new check still reported `62 matched`, `0 new`, and `0 stale`.

During the `cfg_attr` scanner hardening PRs, focused scanner validation,
workspace validation, and no-new dogfood remained clean:

```text
rtk cargo test -p allow-rust unsafe_scan
rtk cargo test -p allow-rust lint
rtk cargo fmt --all --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
```

The latest full workspace test run on the scanner branch passed with `888`
tests across `33` suites. The refreshed no-new checks, including the post-merge
mainline check after the scanner PRs landed, still reported `62 matched`,
`0 new`, and `0 stale`.

After the release candidate was staged, additional scanner identity hardening
merged on `main`:

```text
#1249 rust: preserve unsafe attribute columns
#1250 rust: detect multiple cfg_attr unsafe attributes
#1251 rust: detect multiple cfg_attr lint attributes
#1252 rust: scan spaced attribute tokens
#1253 rust: split multi-lint attributes
#1254 rust: emit multiple index findings per line
#1255 rust: record index receiver identity
#1256 rust: record panic macro path identity
#1257 rust: record unsafe function symbols
#1258 rust: record unsafe trait symbols
```

The latest focused and workspace validation for the post-candidate scanner
identity hardening passed with `902` tests across `33` suites. The merged
mainline no-new check still reported `62 matched`, `0 new`, and `0 stale` over
`543` git-tracked source-tree inventory files.

The full dry-run sequence was started from the post-scanner-hardening candidate
state. The dependency-free package dry-run still passed:

```text
rtk cargo publish --dry-run -p allow-core --locked
```

The next package, `allow-policy`, correctly stopped before verification because
`allow-core 0.1.4` is not yet visible in the crates.io index:

```text
rtk cargo publish --dry-run -p allow-policy --locked
error: failed to select a version for the requirement `allow-core = "^0.1.4"`
candidate versions found which didn't match: 0.1.3, 0.1.2, 0.1.1, ...
```

That means downstream dry-runs remain publication-turn work: each dependent
crate must be dry-run after its internal `0.1.4` predecessors are published and
visible to crates.io resolution. This is not a publication claim.

The package-list command and dependency-free `allow-core` dry-run passed from a
clean `main` tree after the release-candidate version bump merged. Downstream
dry-runs still need to run in publish order during the actual publication turn,
after each predecessor `0.1.4` crate is visible to crates.io resolution.

Additional migration and worklist hardening merged after the publication record
shell was prepared:

```text
#1262 migrate: cover repo-policy lint and unsafe inputs
#1263 migrate: show evidence count in human summary
#1264 migrate: count lint exception entries
#1265 migrate: summarize weak evidence references
#1266 docs: update migrate summary fields
#1267 worklist: specialize unsafe weak evidence actions
#1269 worklist: cover migrated unsafe weak evidence
#1271 migrate: cover no-panic repo policy inputs
#1273 migrate: cover file workflow repo policy inputs
#1275 migrate: cover repo policy json summary
```

The latest focused and workspace validation for this migration/worklist
hardening passed with `907` tests across `33` suites. The merged mainline
no-new check after #1275 still reported `62 matched`, `0 new`, and `0 stale`
over `543` git-tracked source-tree inventory files.

After the documentation split, package metadata was updated so each published
crate uses its crate-local README card instead of inheriting the root product
README. The focused metadata guard and package checks observed during that PR
were:

```text
rtk cargo test -p cargo-allow release_candidate_packages_use_crate_local_readmes
rtk cargo package --list --workspace
rtk cargo publish --dry-run -p allow-core --locked
```

The `allow-core` dry-run packaged and verified successfully, then aborted upload
because it was a dry-run. Downstream crate dry-runs still remain publication-turn
work after each internal predecessor `0.1.4` crate is visible to crates.io
resolution. This is not a publication claim.

Package dry-runs should run in publish order during the actual publication turn,
immediately before publishing each crate after its internal predecessors are
visible:

```bash
rtk cargo publish --dry-run -p <crate> --locked
```

On this Windows machine, linker-heavy validation commands may need `TEMP` and
`TMP` routed to `H:\Temp` because the system temp drive has previously
exhausted space during `link.exe`.

## Required Publication Work

Before publication, the release still needs to:

1. Run the full release preflight:

   ```text
   rtk cargo fmt --all --check
   rtk cargo clippy --workspace --all-targets -- -D warnings
   rtk cargo test --workspace
   rtk cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
   rtk cargo package --list --workspace
   ```

2. Run package dry-runs in dependency order, immediately before publishing each
   crate after its internal predecessors are visible:

   ```bash
   rtk cargo publish --dry-run -p <crate> --locked
   ```

3. Publish only after dry-runs pass, in dependency order:

   ```text
   1. allow-core
   2. allow-policy
   3. allow-inventory
   4. allow-files
   5. allow-rust
   6. allow-match
   7. allow-report
   8. allow-policy-legacy
   9. allow-diff
   10. cargo-allow
   ```

4. Verify registry visibility and the installed binary:

   ```bash
   rtk cargo search cargo-allow --limit 1
   rtk cargo install cargo-allow --version 0.1.4 --locked --root target/cargo-allow-install --force
   rtk .\target\cargo-allow-install\bin\cargo-allow.exe --version
   rtk .\target\cargo-allow-install\bin\cargo-allow.exe --help
   rtk .\target\cargo-allow-install\bin\cargo-allow.exe check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
   ```

5. Update public install examples from `0.1.3` to `0.1.4` only after registry
   visibility and installed-binary verification pass.

## Final Publication Record Template

These sections are intentionally empty until an explicitly authorized publish
turn observes the corresponding facts. Do not fill them from intent, local
dry-runs, or planned commands.

### Published Crates

Not yet observed.

### Registry Visibility

Not yet observed.

### Install Smoke

Not yet observed.

### Post-Publish Verification

Not yet observed.

### Rollback/Yank Instructions

Use the rollback commands below only after a real publication exists. Do not
yank unpublished versions.

### Known Limitations

The 0.1.4 candidate remains source-tree/source-syntax scoped. It does not claim
macro-expanded, type-aware, MIR-level, build-aware, control-flow, data-flow,
unsafe-proof, test-adequacy, or coverage-proof behavior.

## Rollback

Crates.io versions are immutable. If a future `0.1.4` publication is wrong:

```bash
rtk cargo yank <crate> --version 0.1.4
```

Then prepare and publish a patch release. Do not reuse `0.1.4` for corrected
contents.
