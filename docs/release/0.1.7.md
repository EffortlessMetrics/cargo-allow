# 0.1.7 Preview Release Notes

This is a release-candidate record for a future `0.1.7` patch release after the
published `0.1.6` crate set.

Workspace package versions have been bumped to `0.1.7` for release-candidate
validation. No crate has been tagged, published, install-smoke verified, or
verified on crates.io for `0.1.7` yet. Public install examples should keep
pinning the latest published `0.1.6` release until publication completes.

## Target Claim

The 0.1.7 release should keep the 0.1.x default claim:

```text
cargo-allow inventories syntax-visible source exceptions and checks them
against a policy ledger.
```

It may additionally say that cargo-allow includes an opt-in `spec-system`
preview profile for static source-tree graph validation. That profile validates
registered source-of-truth artifacts and their structural relationships without
changing default cargo-allow behavior.

It must not claim that the profile is stable, default-enabled, or proof
executing. It must not claim macro-expanded, type-aware, MIR-level,
build-aware, control-flow, data-flow, unsafe-proof, test-adequacy,
coverage-proof, release-readiness, or semantic-correctness behavior.

## Preview Scope

The preview release notes cover the spec-system PR sequence merged after
0.1.6:

- Source-of-truth stack docs, templates, proposal, spec, support-tier map,
  active goal manifest, implementation plan, closeout, and doc-artifact ledger.
- Internal spec-system config, doc-artifact ledger parsing, artifact file and
  ID validation, graph link validation, support-tier validation, and profile
  mode reporting.
- `check --profile spec-system`, `audit --profile spec-system`, `worklist
  --profile spec-system --format json`, `doctor --profile spec-system`,
  `init --profile spec-system`, and `explain <artifact-id> --profile
  spec-system` preview command surfaces.
- `cargo-allow.spec-system.v1` JSON artifacts with source-tree graph inventory,
  findings, work items, readiness, single-artifact explanation, claim boundary,
  and scanner limitations.
- Spec-system worklist repair items for broken source-of-truth graph structure.
- Active-goal manifest validation for TOML shape, ledger-resolvable links,
  work-item proof-command fields, and done-item closeout references.
- Advisory and shadow CI artifact upload, clean shadow burn-in evidence, and
  repo-local blocking-mode dogfood for selected objective structural finding
  classes.
- Reviewer and agent output polish that separates blocking-eligible structural
  posture from advisory lifecycle posture.
- First-hour adoption and CI guidance for this profile as one opt-in
  governance profile among possible future profiles.
- Opt-in profile architecture and cross-repo adoption guidance, including an
  adoption-friction issue template for portability feedback from downstream
  repositories.

Prepared public GitHub Release notes are in
[docs/release/github/v0.1.7.md](github/v0.1.7.md). They are a prepared release
body only; no GitHub Release exists until the authorized publish/tag step.

## Blocking-Eligible Structural Classes

Blocking mode may fail commands for objective structural failures:

- malformed explicit profile config.
- missing or invalid doc-artifact ledger.
- duplicate artifact IDs.
- invalid artifact kind or status.
- missing registered artifact files.
- unreadable registered artifact files.
- registered artifact files missing their declared IDs.
- unknown linked artifact IDs.

Judgment-heavy lifecycle checks should remain advisory for this preview:

- stale active goals.
- missing closeouts.
- support-tier proof completeness.
- README or release claim coverage.

## Non-Goals

- Do not make spec-system part of default `cargo-allow check`.
- Do not promote the spec-system support-tier row beyond Advisory as part of
  release-note preparation.
- Do not run proof commands from cargo-allow's own scan.
- Do not call GitHub APIs or network services from the scanner.
- Do not invoke Cargo, rustc, Clippy, build scripts, proc macros, ripr,
  unsafe-review, coverage, or repository code from the scanner.
- Do not claim stable support-tier status for the spec-system profile.

## Pre-Publish Checklist

Before publishing `0.1.7`, verify:

- `CHANGELOG.md` has a dated `0.1.7` section and an empty `Unreleased`
  section for future work.
- `docs/release/github/v0.1.7.md` is the intended public GitHub Release body.
- public install examples still pin published `0.1.6` until crates.io publish
  and install-smoke verification complete.
- `docs/status/SUPPORT_TIERS.md` keeps the spec-system profile Advisory.
- no tag named `v0.1.7` exists before the authorized GitHub Release step.
- no package has been published as `0.1.7` before the authorized publish step.
- all pre-release validation commands below have been refreshed on the exact
  candidate commit.

## Pre-Release Evidence To Refresh

Before a real `0.1.7` release, refresh these commands on the exact release
candidate commit:

```bash
rtk cargo fmt --all --check
rtk cargo clippy --workspace --all-targets -- -D warnings
rtk cargo test --workspace
rtk cargo run -p cargo-allow -- doctor --profile spec-system --format json --output target/cargo-allow/spec-system-doctor.json
rtk cargo run -p cargo-allow -- check --profile spec-system --mode audit --format json --output target/cargo-allow/spec-system.json
rtk cargo run -p cargo-allow -- worklist --profile spec-system --format json --output target/cargo-allow/spec-system-worklist.json
rtk cargo run -p cargo-allow -- explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target/cargo-allow/spec-system-explain.json
rtk cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
```

If release authorization is granted, also run per-crate publish dry-runs
immediately before each publish step. Dependent crate publish dry-runs require
their internal `0.1.7` dependencies to be visible in the crates.io index, so
they cannot all pass before the first internal crate is published. Do not
publish from this release-candidate record alone.

## Package Dry-Run Review

Reviewed on 2026-06-12 at release-candidate commit
`59847a7cb570b696e5113006594a14d57df99519`.

| Surface | Result | Evidence |
| --- | --- | --- |
| Workspace package verification | passed | `cargo package --workspace --locked --allow-dirty` packaged and verified all 10 workspace packages as `0.1.7` using Cargo's temporary local registry. |
| First publish dry-run | passed | `cargo publish --dry-run -p allow-core --locked` packaged, verified, and dry-run uploaded `allow-core v0.1.7`; upload was aborted by dry-run as expected. |
| Dependent publish dry-run before publication | blocked as expected | `cargo publish --dry-run -p allow-policy --locked` failed because crates.io does not yet contain `allow-core = "^0.1.7"`. Run dependent crate publish dry-runs immediately before their publish steps after prior internal dependencies are visible in the index. |

## Release-Candidate Review

This release-candidate record bumps the workspace to `0.1.7` and records local
candidate validation. Evidence was refreshed on 2026-06-12 at release-candidate
commit `d9e119f83b43926faf7d74a838ca8e63c520e663`.

This review supports a release-authorization decision for the opt-in
spec-system preview. It is not a release, package, tag, publish, install smoke,
or stable-support claim.

| Surface | Result | Evidence |
| --- | --- | --- |
| Current version state | candidate bumped | Workspace package version, internal workspace dependency requirements, and workspace lockfile entries are `0.1.7`; public install pins remain on published `0.1.6` until publication. |
| GitHub release state | not published | `gh release list --limit 10` returned no GitHub release rows. |
| Git tag state | not tagged | `git ls-remote --tags origin "v0.1.*"` returned no matching remote tags. |
| Latest release-candidate main CI | passed | GitHub Actions run `27438634440` passed on `d9e119f83b43926faf7d74a838ca8e63c520e663`. |
| Release-candidate local package list | passed | `cargo package --list --workspace --allow-dirty` listed the workspace package contents without packaging, tagging, or publishing. |
| Release-candidate local tests | passed | `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` passed on the release-candidate branch; workspace tests reported 1193 passed. |
| Spec-system doctor | passed locally | `cargo-allow doctor --profile spec-system --format json` reported `mode = blocking`, `status = passed`, and `readiness.ready = true`. |
| Spec-system check | passed locally | `cargo-allow check --profile spec-system --mode audit --format json` reported 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 blocking-eligible findings, 0 advisory findings, and 0 work items. |
| Spec-system worklist | passed locally | `cargo-allow worklist --profile spec-system --format json` reported 0 findings and 0 work items. |
| Spec-system explain | passed locally | `cargo-allow explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json` reported `CARGO-ALLOW-SPEC-0001`, 5 links, 0 findings, and 0 work items. |
| Default no-new guard | passed locally | `cargo-allow check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md` reported `status = passed`, 620 files scanned, 113 matched findings, and 0 new findings. |

Release authorization is still required before any package, tag, publish,
install-smoke, public install-doc update, or release-record finalization work.

Before publication, a release PR should still:

- confirm package versions, internal dependency requirements, and lockfile
  entries are still `0.1.7`.
- refresh the evidence above on the exact release candidate commit.
- run `cargo publish --dry-run` immediately before each publish step after
  prior internal dependencies are visible in the crates.io index.
- run any authorized install-smoke checks.
- keep `CARGO-ALLOW-SUPPORT-0001` advisory unless a separate support-tier
  promotion decision is made.

## Claim Boundary

The spec-system profile is structural source-tree graph validation. It may parse
TOML and Markdown and inspect repository files. It may verify IDs, paths,
statuses, links, support-tier proof fields, active-goal references, and closeout
links.

It must not execute proof commands, call GitHub APIs, inspect external PR state,
run Cargo, rustc, Clippy, build scripts, proc macros, ripr, unsafe-review,
coverage, or network checks as part of the cargo-allow scan.

## Rollback

Before publication, rollback is a normal revert of the release-note commit or
any spec-system preview PRs that are not included in the release scope.

After publication, crates.io versions are immutable. If a published package is
wrong:

```bash
rtk cargo yank <crate> --version 0.1.7
```

Then prepare and publish a fixed patch release. Do not reuse `0.1.7` for
corrected contents.
