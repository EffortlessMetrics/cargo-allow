# 0.1.7 Release Record

This records the completed `0.1.7` patch release after the published `0.1.6`
crate set.

The release is irreversible now that packages are published. If a published
package is wrong, the rollback path is to yank the affected version and publish
a fixed patch release. Do not reuse `0.1.7` for corrected contents.

## Target Claim

The 0.1.7 release keeps the 0.1.x default claim:

```text
cargo-allow inventories syntax-visible source exceptions and checks them
against a policy ledger.
```

It may additionally say that cargo-allow includes an opt-in `spec-system`
preview profile for static source-tree graph validation. That profile validates
registered source-of-truth artifacts and their structural relationships without
changing default cargo-allow behavior.

It must not claim that the profile is stable, default-enabled, or proof
executing. It must not claim macro-expanded, type-aware, MIR-level,
build-aware, control-flow, data-flow, unsafe-proof, test-adequacy,
coverage-proof, release-readiness, or semantic-correctness behavior.

## Release Scope

0.1.7 is a patch release for the opt-in spec-system preview profile merged after
0.1.6:

- Source-of-truth stack docs, templates, proposal, spec, support-tier map,
  active goal manifest, implementation plan, closeout, and doc-artifact ledger.
- Internal spec-system config, doc-artifact ledger parsing, artifact file and
  ID validation, graph link validation, support-tier validation, and profile
  mode reporting.
- `check --profile spec-system`, `audit --profile spec-system`, `worklist
  --profile spec-system --format json`, `doctor --profile spec-system`,
  `init --profile spec-system`, and `explain <artifact-id> --profile
  spec-system` preview command surfaces.
- `cargo-allow.spec-system.v1` JSON artifacts with source-tree graph inventory,
  findings, work items, readiness, single-artifact explanation, claim boundary,
  and scanner limitations.
- Spec-system worklist repair items for broken source-of-truth graph structure.
- Active-goal manifest validation for TOML shape, ledger-resolvable links,
  work-item proof-command fields, and done-item closeout references.
- Advisory and shadow CI artifact upload, clean shadow burn-in evidence, and
  repo-local blocking-mode dogfood for selected objective structural finding
  classes.
- Reviewer and agent output polish that separates blocking-eligible structural
  posture from advisory lifecycle posture.
- First-hour adoption and CI guidance for this profile as one opt-in
  governance profile among possible future profiles.
- Opt-in profile architecture and cross-repo adoption guidance, including an
  adoption-friction issue template for portability feedback from downstream
  repositories.

Non-goals for 0.1.7:

- Do not make spec-system part of default `cargo-allow check`.
- Do not promote the spec-system support-tier row beyond Advisory.
- Do not run proof commands from cargo-allow's own scan.
- Do not call GitHub APIs or network services from the scanner.
- Do not invoke Cargo, rustc, Clippy, build scripts, proc macros, ripr,
  unsafe-review, coverage, or repository code from the scanner.
- Do not claim stable support-tier status for the spec-system profile.

## Blocking-Eligible Structural Classes

Blocking mode may fail commands for objective structural failures:

- malformed explicit profile config.
- missing or invalid doc-artifact ledger.
- duplicate artifact IDs.
- invalid artifact kind or status.
- missing registered artifact files.
- unreadable registered artifact files.
- registered artifact files missing their declared IDs.
- unknown linked artifact IDs.

Judgment-heavy lifecycle checks remain advisory for this preview:

- stale active goals.
- missing closeouts.
- support-tier proof completeness.
- README or release claim coverage.

## Release-Candidate Evidence

The final pre-publish candidate was merged to `main` at
`871e8af9fa8dbdc092661c4730af1111074d9d6f` by
`#1520 release: record 0.1.7 package evidence`.

The latest mainline CI before publication passed:

```text
GitHub Actions run 27439539380
head 871e8af9fa8dbdc092661c4730af1111074d9d6f
workflow CI
conclusion success
```

Pre-publish candidate evidence recorded on 2026-06-12 included:

```text
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo run -p cargo-allow -- doctor --profile spec-system --format json --output target/cargo-allow/spec-system-doctor.json
cargo run -p cargo-allow -- check --profile spec-system --mode audit --format json --output target/cargo-allow/spec-system.json
cargo run -p cargo-allow -- worklist --profile spec-system --format json --output target/cargo-allow/spec-system-worklist.json
cargo run -p cargo-allow -- explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target/cargo-allow/spec-system-explain.json
cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
```

The refreshed candidate evidence reported:

```text
workspace tests: 1193 passed
spec-system doctor: mode = blocking, status = passed, readiness.ready = true
spec-system check: 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 work items
spec-system worklist: 0 findings, 0 work items
spec-system explain CARGO-ALLOW-SPEC-0001: 1 artifact, 5 links, 0 findings, 0 work items
default no-new: 620 files scanned, 113 matched findings, 0 new findings
```

## Version and Install Examples

This release updates the workspace package version, internal workspace
dependency requirements, and lockfile package versions from `0.1.6` to `0.1.7`:

```text
Cargo.toml
Cargo.lock
```

Public install examples now pin the published `0.1.7` release:

```bash
cargo install cargo-allow --version 0.1.7 --locked
```

## Package Dry-Run Review

Package evidence was reviewed before publication:

| Surface | Result | Evidence |
| --- | --- | --- |
| Workspace package verification | passed | `cargo package --workspace --locked` packaged and verified all 10 workspace packages as `0.1.7` using Cargo's temporary local registry. |
| First publish dry-run | passed | `cargo publish --dry-run -p allow-core --locked` packaged, verified, and dry-run uploaded `allow-core v0.1.7`; upload was aborted by dry-run as expected. |
| Dependent publish dry-run before publication | blocked as expected | `cargo publish --dry-run -p allow-policy --locked` failed before publication because crates.io did not yet contain `allow-core = "^0.1.7"`. Dependent crate publish dry-runs were run immediately before their publish steps after prior internal dependencies were visible in the index. |

## Publish Order

Published internal crates in dependency order:

```text
1. allow-core
2. allow-policy
3. allow-inventory
4. allow-files
5. allow-rust
6. allow-match
7. allow-report
8. allow-policy-legacy
9. allow-diff
10. cargo-allow
```

Each package was dry-run verified immediately before publishing with:

```bash
cargo publish --dry-run -p <crate> --locked
```

Then published with:

```bash
cargo publish -p <crate> --locked
```

`allow-report` dry-run verification passed, but its first real upload attempt
timed out before publication completed. crates.io still reported
`allow-report = "0.1.6"` afterward, so the publish was retried and completed
successfully.

## Published Registry State

The following crates were verified visible on crates.io:

```text
allow-core 0.1.7
allow-policy 0.1.7
allow-inventory 0.1.7
allow-files 0.1.7
allow-rust 0.1.7
allow-match 0.1.7
allow-report 0.1.7
allow-policy-legacy 0.1.7
allow-diff 0.1.7
cargo-allow 0.1.7
```

Registry visibility was checked with:

```bash
cargo search cargo-allow --limit 20
cargo search allow-core --limit 20
cargo search allow-policy --limit 20
cargo search allow-inventory --limit 20
cargo search allow-files --limit 20
cargo search allow-rust --limit 20
cargo search allow-match --limit 20
cargo search allow-report --limit 20
cargo search allow-policy-legacy --limit 20
cargo search allow-diff --limit 20
```

The final `cargo-allow` dry-run resolved every internal `0.1.7` crate from
crates.io before publication.

## Final Verification

After `cargo-allow` was published, the release was verified with the installed
crates.io binary:

```bash
cargo install cargo-allow --version 0.1.7 --locked --root target/cargo-allow-install --force
.\target\cargo-allow-install\bin\cargo-allow.exe --version
.\target\cargo-allow-install\bin\cargo-allow.exe --help
.\target\cargo-allow-install\bin\cargo-allow.exe doctor --profile spec-system --help
.\target\cargo-allow-install\bin\cargo-allow.exe doctor --profile spec-system --format json --output target/cargo-allow/install-spec-system-doctor.json
.\target\cargo-allow-install\bin\cargo-allow.exe check --profile spec-system --mode audit --format json --output target/cargo-allow/install-spec-system.json
.\target\cargo-allow-install\bin\cargo-allow.exe worklist --profile spec-system --format json --output target/cargo-allow/install-spec-system-worklist.json
.\target\cargo-allow-install\bin\cargo-allow.exe explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target/cargo-allow/install-spec-system-explain.json
.\target\cargo-allow-install\bin\cargo-allow.exe check --mode no-new --format markdown --receipt target/cargo-allow/install-check.receipt.json --output target/cargo-allow/install-check.md
```

The installed binary reported `cargo-allow 0.1.7`.

The installed-binary spec-system checks reported:

```text
doctor: mode = blocking, status = passed, readiness.ready = true
check: mode = blocking, status = passed, 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 work items
worklist: status = passed, 0 findings, 0 work items
explain CARGO-ALLOW-SPEC-0001: status = passed, 1 artifact, 5 links, 0 findings, 0 work items
```

The installed-binary default no-new check passed with `620` files scanned,
`113` matched findings, and `0` new findings.

## GitHub Release

The public GitHub Release body is prepared in
[docs/release/github/v0.1.7.md](github/v0.1.7.md). The `v0.1.7` tag and
GitHub Release should be created from the final release-record commit after
this record merges.

## Known Limitations

The 0.1.7 release remains source-tree/source-syntax scoped by default. The
opt-in spec-system profile is structural source-tree graph validation. It may
parse TOML and Markdown and inspect repository files. It may verify IDs, paths,
statuses, links, support-tier proof fields, active-goal references, and closeout
links.

It must not execute proof commands, call GitHub APIs, inspect external PR state,
run Cargo, rustc, Clippy, build scripts, proc macros, ripr, unsafe-review,
coverage, or network checks as part of the cargo-allow scan.

## Rollback

Crates.io versions are immutable. If publication is wrong:

```bash
cargo yank <crate> --version 0.1.7
```

Then prepare and publish a fixed patch release. Do not reuse `0.1.7` for
corrected contents.
