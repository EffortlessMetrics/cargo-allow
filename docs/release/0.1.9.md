# 0.1.9 Release Record

This records the completed `0.1.9` patch release after the published `0.1.8`
crate set.

The release is irreversible now that packages are published. If a published
package is wrong, the rollback path is to yank the affected version and publish
a fixed patch release. Do not reuse `0.1.9` for corrected contents.

## Target Claim

The 0.1.9 release keeps the 0.1.x default claim:

```text
cargo-allow inventories syntax-visible source exceptions and checks them
against a policy ledger.
```

It additionally hardens the adoption-quality receipt path for gate consumers:
`review_due` counts, exit-2 error receipts, ledger parse ergonomics, and
receipt run provenance (`mode`, `enforcement`, `policy_config`, `tool_version`).

It must not claim new scanner features, macro-expanded analysis, proof execution,
or semantic-correctness behavior.

## Release Scope

0.1.9 is a maintenance and adoption patch:

- Receipt fixes for gate consumers (`#1465`, `#1467`, `#1468`, `#1469`).
- Matcher lifecycle alignment so `check`, `list`, and `worklist` agree on
  `review_due` posture.
- Test hardening and module decomposition carried in the 0.1.9 lane stack.
- ripr `0.10.0` readiness validation and fixture alignment only.

## Non-Goals

- Do not add new scanner identity lanes in this patch.
- Do not claim spec-system promotion beyond the existing preview boundary.
- Do not claim macro-expanded, type-aware, MIR-level, build-aware,
  control-flow, data-flow, unsafe-proof, test-adequacy, or coverage-proof
  behavior.

## Release-Candidate Evidence

The final pre-publish candidate was merged to `main` at
`7d47f4a99c37a31ed658bb756e2d0a8018c69778` by
`#1679 0.1.9: receipt adoption fixes and release staging`.

The latest mainline CI before publication passed:

```text
GitHub Actions run 27592865032
head 7d47f4a99c37a31ed658bb756e2d0a8018c69778
workflow CI
conclusion success
```

Pre-publish candidate evidence on 2026-06-16 included:

```text
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo run -p cargo-allow -- doctor --profile spec-system --format json --output target/cargo-allow/spec-system-doctor.json
cargo run -p cargo-allow -- check --profile spec-system --mode audit --format json --output target/cargo-allow/spec-system.json
cargo run -p cargo-allow -- worklist --profile spec-system --format json --output target/cargo-allow/spec-system-worklist.json
cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
```

The refreshed candidate evidence reported:

```text
workspace tests: 1712 passed
spec-system doctor: mode = blocking, status = passed, readiness.ready = true
default no-new: 659 files scanned, 120 matched findings, 0 new findings
```

`cargo semver-checks --workspace --baseline-version 0.1.8` reported one
advisory failure in `allow-report`: pub `ReportContext` fields
`mode`, `enforcement`, `policy_config`, and `tool_version` were added for
receipt provenance. The tool classified that as a major-semver change; the
release lane accepted it as an intentional 0.1.x receipt contract extension for
internal crates. Nine other workspace crates reported no semver update required.

## Version and Install Examples

Workspace package versions were bumped to `0.1.9`:

```text
Cargo.toml
Cargo.lock
```

Public install examples now pin the published `0.1.9` release:

```bash
cargo install cargo-allow --version 0.1.9 --locked
```

## Package Dry-Run Review

Package evidence was reviewed before publication:

| Surface | Result | Evidence |
| --- | --- | --- |
| Workspace package verification | passed | `cargo package --workspace --locked` packaged and verified all 10 workspace packages as `0.1.9` using Cargo's temporary local registry. |
| Per-crate publish dry-runs | passed | Each package was dry-run verified immediately before its real publish step after any internal dependencies were visible in the crates.io index. |

## Publish Order

Published internal crates in dependency order:

```text
1. allow-core
2. allow-policy
3. allow-inventory
4. allow-files
5. allow-rust
6. allow-match
7. allow-policy-legacy
8. allow-report
9. allow-diff
10. cargo-allow
```

Each package was dry-run verified immediately before publishing with:

```bash
cargo publish --dry-run -p <crate> --locked
```

Then published with:

```bash
cargo publish -p <crate> --locked
```

## Published Registry State

The following crates were verified visible on crates.io:

```text
allow-core 0.1.9
allow-policy 0.1.9
allow-inventory 0.1.9
allow-files 0.1.9
allow-rust 0.1.9
allow-match 0.1.9
allow-policy-legacy 0.1.9
allow-report 0.1.9
allow-diff 0.1.9
cargo-allow 0.1.9
```

Registry visibility was checked with:

```bash
cargo search cargo-allow --limit 20
cargo search allow-core --limit 20
```

The final `cargo-allow` dry-run resolved every internal `0.1.9` crate from
crates.io before publication.

## Final Verification

After `cargo-allow` was published, the release was verified with the installed
crates.io binary:

```bash
cargo install cargo-allow --version 0.1.9 --locked --root target/cargo-allow-0.1.9-install --force
.\target\cargo-allow-0.1.9-install\bin\cargo-allow.exe --version
.\target\cargo-allow-0.1.9-install\bin\cargo-allow.exe doctor --profile spec-system --format json --output target\cargo-allow\install-0.1.9-spec-system-doctor.json
.\target\cargo-allow-0.1.9-install\bin\cargo-allow.exe check --profile spec-system --mode audit --format json --output target\cargo-allow\install-0.1.9-spec-system.json
.\target\cargo-allow-0.1.9-install\bin\cargo-allow.exe worklist --profile spec-system --format json --output target\cargo-allow\install-0.1.9-spec-system-worklist.json
.\target\cargo-allow-0.1.9-install\bin\cargo-allow.exe explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target\cargo-allow\install-0.1.9-spec-system-explain.json
.\target\cargo-allow-0.1.9-install\bin\cargo-allow.exe check --mode no-new --format markdown --receipt target\cargo-allow\install-0.1.9-check.receipt.json --output target\cargo-allow\install-0.1.9-check.md
```

The installed binary reported `cargo-allow 0.1.9`.

The installed-binary spec-system checks reported:

```text
doctor: mode = blocking, status = passed, readiness.ready = true
check: mode = blocking, status = passed, 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 work items
worklist: status = passed, 0 findings, 0 work items
explain CARGO-ALLOW-SPEC-0001: status = passed, 1 artifact, 5 links, 0 findings, 0 work items
```

The installed-binary default no-new check passed with `659` files scanned,
`120` matched findings, and `0` new findings. The receipt recorded
`tool_version = 0.1.9`, `mode = no-new`, `enforcement = enforcing`, and
`review_due = 0`.

## GitHub Release

The public GitHub Release body is recorded in
[docs/release/github/v0.1.9.md](github/v0.1.9.md). The `v0.1.9` tag and
GitHub Release were created after publication:

```text
GitHub Release: https://github.com/EffortlessMetrics/cargo-allow/releases/tag/v0.1.9
published: 2026-06-16T04:04:39Z
status: public, non-draft, non-prerelease
```

## Known Limitations

The 0.1.9 release remains source-tree/source-syntax scoped by default. It
does not execute repository code, Cargo, rustc, Clippy, build scripts, proc
macros, ripr, unsafe-review, coverage, or network checks as part of the scan.

## Rollback

Crates.io versions are immutable. If publication is wrong:

```bash
cargo yank <crate> --version 0.1.9
```

Then prepare and publish a fixed patch release. Do not reuse `0.1.9` for
corrected contents.
