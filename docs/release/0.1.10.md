# 0.1.10 Release Record

This records the completed `0.1.10` patch release after the published `0.1.9`
crate set.

The release is irreversible once packages are published. If a published package
is wrong, the rollback path is to yank the affected version and publish a fixed
patch release. Do not reuse `0.1.10` for corrected contents.

## Target Claim

The 0.1.10 release keeps the 0.1.x default claim:

```text
cargo-allow inventories syntax-visible source exceptions and checks them
against a policy ledger.
```

It improves adoption trust, source-truth portability, release automation, and
scanner reliability for source-tree exception ledgers. It must not claim
macro-expanded analysis, type-aware analysis, MIR-level analysis, build-aware
analysis, control-flow or data-flow proof, proof-tool execution, or test
adequacy.

## Release Scope

0.1.10 is a maintenance and adoption-trust patch:

- Portable governance substrate and `.allow/` profile migration groundwork.
- Migration parity and import-adapter characterization for the `spec-system`
  preview path.
- Multi-ledger federation, mirror-divergence visibility, and ledger-coherence
  change-control foundations.
- Diff movement/posture classification and required-change-note enforcement.
- First-hour adoption docs, CI examples, and release automation hardening.
- Security, inventory, matcher, refresh, schema, report, CLI, and scanner
  regression fixes accumulated after 0.1.9.

## Non-Goals

- Do not cut `0.2.0`.
- Do not raise the Rust MSRV; Rust 1.95 remains a follow-up lane.
- Do not claim full import mode, external ripr migration, or release readiness
  for unrelated downstream repositories.
- Do not claim macro-expanded, type-aware, MIR-level, build-aware,
  control-flow, data-flow, unsafe-proof, test-adequacy, or coverage-proof
  behavior.

## Release-Candidate Evidence

The final pre-publish candidate was merged to `main` at
`5fb9790aa8d11ed6d37c9e6a8901edc0cb4bd542` by
`#2113 Stage 0.1.10 patch release`.

Local pre-publish candidate evidence included:

```text
cargo fmt --all --check
cargo check --workspace --all-targets
cargo test -p cargo-allow release_prep_tests
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo package --workspace --locked
cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
cargo run -p cargo-allow -- worklist --broken-evidence --format json --output target/cargo-allow/broken-evidence.json
bash scripts/release-version-preflight.sh 0.1.10
```

The release workflow dry-run on `main` passed:

```text
GitHub Actions run 28984106076
head 5fb9790aa8d11ed6d37c9e6a8901edc0cb4bd542
event workflow_dispatch
preflight passed
publish dry-run passed
publish receipt auth = secret
```

## Version and Install Examples

Workspace package versions were bumped to `0.1.10`:

```text
Cargo.toml
Cargo.lock
```

Public install examples now pin the published `0.1.10` release:

```bash
cargo install cargo-allow --version 0.1.10 --locked
```

## Publish Order

Published internal crates in dependency order:

```text
1. allow-core
2. allow-policy
3. allow-inventory
4. allow-files
5. allow-rust
6. allow-match
7. allow-policy-legacy
8. allow-report
9. allow-diff
10. cargo-allow
```

## Package Dry-Run Review

Package evidence was recorded by the local candidate check and by the release
workflow preflight jobs:

```text
cargo package --workspace --locked
```

The command packaged and verified all ten workspace packages as `0.1.10`.

The first tag-triggered release workflow passed preflight, including workspace
packaging and the default no-new guard:

```text
GitHub Actions run 28984206339
head 5fb9790aa8d11ed6d37c9e6a8901edc0cb4bd542
event push
ref v0.1.10
preflight passed
publish failed after uploading allow-core 0.1.10
```

The publish failure was a workflow bug in
`scripts/verify-crate-registry-version.sh`: GitHub Actions set
`CARGO_TERM_COLOR=always`, so `cargo search` returned colored output and the
exact-prefix check failed even though crates.io showed `allow-core = "0.1.10"`.

Recovery landed in `#2114 Fix release publish recovery` at
`5ffc75c668f04d66bd758cb16327aff66d9b9bb8`. The recovery workflow skips exact
crate versions already visible on crates.io and forces colorless registry
search output. The explicit recovery dispatch passed:

```text
GitHub Actions run 28984750352
head 5ffc75c668f04d66bd758cb16327aff66d9b9bb8
event workflow_dispatch
preflight passed
publish passed
publish receipt auth = secret
```

Trusted Publishing authentication reported no crates.io Trusted Publishing
configuration for `EffortlessMetrics/cargo-allow`; the workflow used the
documented `CARGO_REGISTRY_TOKEN` fallback. Follow-up:
[#2115](https://github.com/EffortlessMetrics/cargo-allow/issues/2115).

## Published Registry State

The following crates were verified visible on crates.io:

```text
allow-core 0.1.10
allow-policy 0.1.10
allow-inventory 0.1.10
allow-files 0.1.10
allow-rust 0.1.10
allow-match 0.1.10
allow-policy-legacy 0.1.10
allow-report 0.1.10
allow-diff 0.1.10
cargo-allow 0.1.10
```

Registry visibility was checked with:

```bash
bash scripts/verify-crate-registry-version.sh <crate> 0.1.10
cargo search cargo-allow --limit 10
```

## Final Verification

The successful recovery publish ran from `workflow_dispatch`, so the
tag-triggered install-smoke job was skipped. Final install smoke was run locally
against the published crates.io binary:

```bash
cargo +stable install cargo-allow --version 0.1.10 --locked --root target/cargo-allow-0.1.10-install --force
.\target\cargo-allow-0.1.10-install\bin\cargo-allow.exe --version
.\target\cargo-allow-0.1.10-install\bin\cargo-allow.exe doctor
.\target\cargo-allow-0.1.10-install\bin\cargo-allow.exe check --help
.\target\cargo-allow-0.1.10-install\bin\cargo-allow.exe doctor --profile spec-system --help
```

The installed binary reported `cargo-allow 0.1.10`.

The installed-binary `doctor` command reported:

```text
config status: valid
broken evidence links: 0
weak evidence/link references: 0
inventory: source_tree/source_syntax via git_tracked; files scanned: 885
federation config status: valid
configured ledgers: 2
```

## GitHub Release

The public GitHub Release body is recorded in
[docs/release/github/v0.1.10.md](github/v0.1.10.md). Because the successful
publish completed through recovery `workflow_dispatch`, the GitHub Release was
created manually after install smoke passed:

```text
GitHub Release: https://github.com/EffortlessMetrics/cargo-allow/releases/tag/v0.1.10
published: 2026-07-09T00:19:59Z
status: public, non-draft, non-prerelease
```

## Known Limitations

The 0.1.10 release remains source-tree/source-syntax scoped by default. It does
not execute repository code, Cargo, rustc, Clippy, build scripts, proc macros,
ripr, unsafe-review, coverage, or network checks as part of the scan.

## Rollback

Crates.io versions are immutable. If publication is wrong:

```bash
cargo yank <crate> --version 0.1.10
```

Then prepare and publish a fixed patch release. Do not reuse `0.1.10` for
corrected contents.
