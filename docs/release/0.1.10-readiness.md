# 0.1.10 Readiness Assessment

Honest checklist against the adoption-trust release bar in
[CARGO-ALLOW-PROP-0006](../../docs/proposals/CARGO-ALLOW-PROP-0006-adoption-trust-0.1.10.md)
and [plans/release/0.1.10-implementation-plan.md](../../plans/release/0.1.10-implementation-plan.md).

**Assessed at:** `main` after import-parity execution lane closeout
(CARGO-ALLOW-CLOSEOUT-0004; merges through [#1741](https://github.com/EffortlessMetrics/cargo-allow/pull/1741)).

**Release status:** **deferred.** The cut is not the active execution lane.
Post-import next lane (`post-import-d8`, `post-import-1472` ready in
[`.codex/goals/active.toml`](../../.codex/goals/active.toml)) takes priority.
Operator OIDC setup and workflow dry-run are **not blocking** normal PR work on
`main`.

**Claim boundary:** assessment and evidence links only. No version bump, tag, or
publish authorization implied.

## Checklist

| Item | Status | Evidence |
| --- | --- | --- |
| Active goal and plans reflect reality | **done** | Import-parity (#1713–#1718) and adoption-substrate lanes closed; post-import next lane registered with D8 and #1472 `ready` in [`.codex/goals/active.toml`](../../.codex/goals/active.toml); [gap-inventory.md](../../plans/migration-parity/gap-inventory.md) reconciled after CLOSEOUT-0004. |
| Migration gap inventory populated | **done** | [plans/migration-parity/gap-inventory.md](../../plans/migration-parity/gap-inventory.md) — per-lane `partial`/`closed`/`gap` rows with owners and PR links; #1470 closed; #1466 execution lane closed, umbrella open. |
| Legacy fixture matrix exists | **done** | [#1693](https://github.com/EffortlessMetrics/cargo-allow/pull/1693) (merge `cd0ab7b`): `tests/fixtures/migration/`, `migration_fixture_matrix_tests.rs` in `allow-policy-legacy`. |
| One side-by-side migration receipt passes | **done** | [#1697](https://github.com/EffortlessMetrics/cargo-allow/pull/1697) (merge `26a6873`): [docs/dogfood/cargo-allow-panic-baseline.md](../dogfood/cargo-allow-panic-baseline.md) — scoped panic-baseline compat/migrate/canonical/worklist/closeout proof for this repo. |
| Foreign `policy/allow.toml` no longer blocks discovery | **done** | [#1699](https://github.com/EffortlessMetrics/cargo-allow/pull/1699) (merge `53ea19aa`); [#1700](https://github.com/EffortlessMetrics/cargo-allow/pull/1700); [#1470](https://github.com/EffortlessMetrics/cargo-allow/issues/1470) closed. Import-mode parity ([#1466](https://github.com/EffortlessMetrics/cargo-allow/issues/1466)) remains open umbrella for full adoption. |
| Import parity characterization (#1713–#1718) | **done** | CARGO-ALLOW-CLOSEOUT-0004; ripr-style in-repo dogfood [docs/dogfood/cargo-allow-ripr-style-adoption.md](../dogfood/cargo-allow-ripr-style-adoption.md); umbrella #1466 stays `partial` for full import mode. |
| Advisory ratcheting substrate | **done** | Receipt `advisory` counters (pr-006); `check --deny <status>` escalation (pr-007); per-lane `[lanes.<kind>]` posture (pr-008). `occurrence_headroom` deferred — #1472 queued as `post-import-1472`. |
| Identity gaps characterized with fixtures | **done** | D1–D7 closed: [plans/structural-identity/gap-inventory.md](../../plans/structural-identity/gap-inventory.md); D6 matcher (#1730) and D7 diff posture (#1732) landed. D8 limitation docs deferred — queued as `post-import-d8`. |
| Release workflow candidate-safe and version-safe | **done** | [#1703](https://github.com/EffortlessMetrics/cargo-allow/pull/1703) `release-version-preflight.sh`; [#1704](https://github.com/EffortlessMetrics/cargo-allow/pull/1704) dry-run semantics + exact registry visibility; [#1705](https://github.com/EffortlessMetrics/cargo-allow/pull/1705) post-publish install-smoke. Operator docs: [docs/release/README.md](README.md). |
| Trusted Publishing configured | **deferred** (operator) | Documented per-crate checklist in [docs/release/README.md#cratesio-trusted-publishing-preferred](README.md#cratesio-trusted-publishing-preferred). Not required for post-import lanes; needed only when release cut is authorized. |
| Workflow dry-run receipt recorded | **deferred** (operator) | PR E2 in [0.1.10-implementation-plan.md](../../plans/release/0.1.10-implementation-plan.md) — run when release cut is authorized, not during post-import lane. |
| fmt / clippy / tests / docs / no-new / spec-system pass | **done** | CI green on recent merges through #1741. |

## What remains before cut

### Active engineering lane (not release)

- **Post-import D8** — scanner limitation examples and claim-boundary docs
  (`post-import-d8`, ready).
- **#1472 occurrence_headroom** — receipt advisory counts, worklist routing, and
  optional `--deny occurrence_headroom` (`post-import-1472`, ready).
- **Per-lane `partial` migration rows** — additional side-by-side dogfood beyond
  panic-baseline, unsafe-allowlist, and ripr-style batch still open across compat
  kinds.

### Blocked follow-ups (not active execution)

- **[#1473](https://github.com/EffortlessMetrics/cargo-allow/issues/1473) P2**
  multi-ledger federation — design-first; blocked until proposal/spec acceptance
  (`post-import-1473`).
- **[#1466](https://github.com/EffortlessMetrics/cargo-allow/issues/1466)** full
  import mode — higher risk; blocked on external adoption need
  (`post-import-1466`).

### Operator steps (when release is authorized)

1. **Trusted Publishing** — configure OIDC on all ten published crates per
   [docs/release/README.md](README.md), or approve and verify `CARGO_REGISTRY_TOKEN`
   fallback for the first automated cut.
2. **Release workflow dry-run (PR E2)** — run `workflow_dispatch` on
   [`.github/workflows/release.yml`](../../.github/workflows/release.yml) from `main`;
   confirm preflight + publish dry-run green; download `release-publish-receipt`
   artifact; record workflow run id in release prep notes.

### Issue disposition (non-blocking for deferred cut)

- **[#1466](https://github.com/EffortlessMetrics/cargo-allow/issues/1466)** — import
  parity execution lane closed (CLOSEOUT-0004); umbrella remains open for full
  import mode. Explicitly out of `0.1.10` claims per CARGO-ALLOW-PROP-0006.
- **Structural identity D8** — limitation docs queued; not blocking deferred cut.

### Release lane (requires explicit authorization)

- **PR E3** — version bump, `[Unreleased]` → `0.1.10` CHANGELOG section,
  `docs/release/0.1.10.md`, `docs/release/github/v0.1.10.md`.
- **PR E4** — annotated tag `v0.1.10` push after E1–E3 + operator prerequisites.
- **PR E5** — post-publish closeout with workflow run, registry checks, install-smoke
  receipt artifact.

## Summary

Code and documentation prerequisites for an adoption-trust **0.1.10** cut are
largely in place on `main`. Import-parity and adoption-substrate lanes are
closed; advisory ratcheting substrate (receipt counts, `--deny`, per-lane
posture) is landed with `occurrence_headroom` (#1472) as the next ready slice.
The cut is **deferred** pending post-import documentation and ratcheting work.
Release automation (#1703–#1705) is dormant until explicit authorization.
Operator OIDC and dry-run steps are not blocking normal PR work.
