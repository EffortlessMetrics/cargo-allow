# 0.1.10 Readiness Assessment

Honest checklist against the adoption-trust release bar in
[CARGO-ALLOW-PROP-0006](../../docs/proposals/CARGO-ALLOW-PROP-0006-adoption-trust-0.1.10.md)
and [plans/release/0.1.10-implementation-plan.md](../../plans/release/0.1.10-implementation-plan.md).

**Assessed at:** `main` after migration-parity/adoption-substrate reconciliation
(merges through [#1707](https://github.com/EffortlessMetrics/cargo-allow/pull/1707)).

**Release status:** **deferred.** The cut is not the active execution lane.
Adoption-substrate modularization (PRs 2–6 in `.codex/goals/active.toml`) takes
priority. Operator OIDC setup and workflow dry-run are **not blocking** normal
PR work on `main`.

**Claim boundary:** assessment and evidence links only. No version bump, tag, or
publish authorization implied.

## Checklist

| Item | Status | Evidence |
| --- | --- | --- |
| Active goal and plans reflect reality | **done** | B1–B6 marked done; adoption-substrate PR 2 `ready` in [`.codex/goals/active.toml`](../../.codex/goals/active.toml); [gap-inventory.md](../../plans/migration-parity/gap-inventory.md) and [pr-queue.md](../../plans/migration-parity/pr-queue.md) reconciled 2026-06-18. |
| Migration gap inventory populated | **done** | [plans/migration-parity/gap-inventory.md](../../plans/migration-parity/gap-inventory.md) — per-lane `partial`/`closed`/`gap` rows with owners and PR links; #1470 closed; #1466 split tracked. |
| Legacy fixture matrix exists | **done** | [#1693](https://github.com/EffortlessMetrics/cargo-allow/pull/1693) (merge `cd0ab7b`): `tests/fixtures/migration/`, `migration_fixture_matrix_tests.rs` in `allow-policy-legacy`. |
| One side-by-side migration receipt passes | **done** | [#1697](https://github.com/EffortlessMetrics/cargo-allow/pull/1697) (merge `26a6873`): [docs/dogfood/cargo-allow-panic-baseline.md](../dogfood/cargo-allow-panic-baseline.md) — scoped panic-baseline compat/migrate/canonical/worklist/closeout proof for this repo. |
| Foreign `policy/allow.toml` no longer blocks discovery | **done** | [#1699](https://github.com/EffortlessMetrics/cargo-allow/pull/1699) (merge `53ea19aa`); [#1700](https://github.com/EffortlessMetrics/cargo-allow/pull/1700); [#1470](https://github.com/EffortlessMetrics/cargo-allow/issues/1470) closed. Import-mode parity ([#1466](https://github.com/EffortlessMetrics/cargo-allow/issues/1466)) remains open for full adoption. |
| Identity gaps characterized with fixtures | **partial** | D1 inventory: [plans/structural-identity/gap-inventory.md](../../plans/structural-identity/gap-inventory.md). D2 refactor-pair matrix: [#1701](https://github.com/EffortlessMetrics/cargo-allow/pull/1701) (merge `2165848`), `tests/fixtures/structural-identity/`. D6 (match selectors) and D7 (diff posture) still **open**. |
| Release workflow candidate-safe and version-safe | **done** | [#1703](https://github.com/EffortlessMetrics/cargo-allow/pull/1703) `release-version-preflight.sh`; [#1704](https://github.com/EffortlessMetrics/cargo-allow/pull/1704) dry-run semantics + exact registry visibility; [#1705](https://github.com/EffortlessMetrics/cargo-allow/pull/1705) post-publish install-smoke. Operator docs: [docs/release/README.md](README.md). |
| Trusted Publishing configured | **deferred** (operator) | Documented per-crate checklist in [docs/release/README.md#cratesio-trusted-publishing-preferred](README.md#cratesio-trusted-publishing-preferred). Not required for adoption-substrate PRs; needed only when release cut is authorized. |
| Workflow dry-run receipt recorded | **deferred** (operator) | PR E2 in [0.1.10-implementation-plan.md](../../plans/release/0.1.10-implementation-plan.md) — run when release cut is authorized, not during adoption lane. |
| fmt / clippy / tests / docs / no-new / spec-system pass | **done** | CI green on recent merges (#1703–#1707). |

## What remains before cut

### Active engineering lane (not release)

- **Adoption-substrate PRs 2–6** — migration lane descriptors, evidence/lifecycle
  helpers, closeout normalization, #1466 governance split, advisory counts.
- **Per-lane `partial` migration rows** — side-by-side dogfood and full lane
  acceptance still open across compat kinds.

### Operator steps (when release is authorized)

1. **Trusted Publishing** — configure OIDC on all ten published crates per
   [docs/release/README.md](README.md), or approve and verify `CARGO_REGISTRY_TOKEN`
   fallback for the first automated cut.
2. **Release workflow dry-run (PR E2)** — run `workflow_dispatch` on
   [`.github/workflows/release.yml`](../../.github/workflows/release.yml) from `main`;
   confirm preflight + publish dry-run green; download `release-publish-receipt`
   artifact; record workflow run id in release prep notes.

### Issue disposition (non-blocking for deferred cut)

- **[#1466](https://github.com/EffortlessMetrics/cargo-allow/issues/1466)** — bespoke
  semantic-selector import/parity remains **open umbrella**. Governance split
  tracked in `adoption-substrate-pr-005`; explicitly out of `0.1.10` claims
  per CARGO-ALLOW-PROP-0006.
- **Structural identity D6/D7** — match-selector and diff-posture characterization
  open; tracked for 0.4.0 hardening lane.

### Release lane (requires explicit authorization)

- **PR E3** — version bump, `[Unreleased]` → `0.1.10` CHANGELOG section,
  `docs/release/0.1.10.md`, `docs/release/github/v0.1.10.md`.
- **PR E4** — annotated tag `v0.1.10` push after E1–E3 + operator prerequisites.
- **PR E5** — post-publish closeout with workflow run, registry checks, install-smoke
  receipt artifact.

## Summary

Code and documentation prerequisites for an adoption-trust **0.1.10** cut are
largely in place on `main`. The cut is **deferred** pending adoption-substrate
cleanup/modularization lanes. Release automation (#1703–#1705) is dormant until
explicit authorization. Operator OIDC and dry-run steps are not blocking normal
PR work. Issue [#1466](https://github.com/EffortlessMetrics/cargo-allow/issues/1466)
should be split in `adoption-substrate-pr-005` before any future cut if it
remains open.
