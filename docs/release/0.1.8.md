# 0.1.8 Release Record

This records the completed `0.1.8` patch release after the published `0.1.7`
crate set.

The release is irreversible now that packages are published. If a published
package is wrong, the rollback path is to yank the affected version and publish
a fixed patch release. Do not reuse `0.1.8` for corrected contents.

## Target Claim

The 0.1.8 release keeps the 0.1.x default claim:

```text
cargo-allow inventories syntax-visible source exceptions and checks them
against a policy ledger.
```

It may additionally say that the opt-in `spec-system` preview profile has a
smoother first-hour adoption path: generated profile files no longer create an
immediate active-goal readiness finding before a repository has registered a
real proposal/spec/plan graph.

It must not claim that the profile is stable, default-enabled, or proof
executing. It must not claim macro-expanded, type-aware, MIR-level,
build-aware, control-flow, data-flow, unsafe-proof, test-adequacy,
coverage-proof, release-readiness, or semantic-correctness behavior.

## Release Scope

0.1.8 is a cleanup patch for friction found while self-hosting the published
0.1.7 binary:

- `cargo-allow init --profile spec-system` now generates
  `active_goal_required = false` in `policy/spec-system.toml`, so a new
  repository can get a clean first-hour `doctor --profile spec-system` result
  before it has registered real proposal/spec/plan/support-tier/closeout
  artifacts.
- Generated `.codex/goals/active.toml` explains that it is placeholder
  execution state and that active-goal enforcement should be enabled after the
  real graph is registered.
- `doctor --profile spec-system` reports the active-goal readiness check as
  ready when `active_goal_required = false`, with a message explaining that
  validation is optional.
- Spec-system Markdown reports use one neutral `Findings` section and a
  `Findings` metric row instead of repeating mode-specific headings such as
  `Advisory Findings`.
- Adoption docs explain when to flip `active_goal_required = true`.

## Non-Goals

- Do not make spec-system part of default `cargo-allow check`.
- Do not promote the spec-system support-tier row beyond Advisory.
- Do not add new lint rules in this release.
- Do not run proof commands from cargo-allow's own scan.
- Do not call GitHub APIs or network services from the scanner.
- Do not invoke Cargo, rustc, Clippy, build scripts, proc macros, ripr,
  unsafe-review, coverage, or repository code from the scanner.
- Do not claim stable support-tier status for the spec-system profile.

## Release-Candidate Evidence

The final pre-publish candidate was merged to `main` at
`2f2e33314a24427d5c30976ca7b3246b2e214218` by
`#1523 release: stage 0.1.8 spec-system cleanup`.

The latest mainline CI before publication passed:

```text
GitHub Actions run 27443560286
head 2f2e33314a24427d5c30976ca7b3246b2e214218
workflow CI
conclusion success
```

Pre-publish candidate evidence refreshed on 2026-06-12 included:

```text
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo run -p cargo-allow -- doctor --profile spec-system --format json --output target/cargo-allow/spec-system-doctor.json
cargo run -p cargo-allow -- check --profile spec-system --mode audit --format json --output target/cargo-allow/spec-system.json
cargo run -p cargo-allow -- worklist --profile spec-system --format json --output target/cargo-allow/spec-system-worklist.json
cargo run -p cargo-allow -- explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target/cargo-allow/spec-system-explain.json
cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md
```

The refreshed candidate evidence reported:

```text
workspace tests: 1194 passed
spec-system doctor: mode = blocking, status = passed, readiness.ready = true
spec-system check: 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 work items
spec-system worklist: 0 findings, 0 work items
spec-system explain CARGO-ALLOW-SPEC-0001: 1 artifact, 5 links, 0 findings, 0 work items
default no-new: 622 files scanned, 115 matched findings, 0 new findings
```

## Version and Install Examples

Workspace package versions were bumped to `0.1.8`:

```text
Cargo.toml
Cargo.lock
```

Public install examples now pin the published `0.1.8` release:

```bash
cargo install cargo-allow --version 0.1.8 --locked
```

## Package Dry-Run Review

Package evidence was reviewed before publication:

| Surface | Result | Evidence |
| --- | --- | --- |
| Workspace package verification | passed | `cargo package --workspace --locked` packaged and verified all 10 workspace packages as `0.1.8` using Cargo's temporary local registry. |
| First publish dry-run | passed | `cargo publish --dry-run -p allow-core --locked` packaged, verified, and dry-run uploaded `allow-core v0.1.8`; upload was aborted by dry-run as expected. |
| Per-crate publish dry-runs | passed | Each package was dry-run verified immediately before its real publish step after any internal dependencies were visible in the crates.io index. |

## Publish Order

Published internal crates in dependency order:

```text
1. allow-core
2. allow-policy
3. allow-inventory
4. allow-files
5. allow-rust
6. allow-match
7. allow-report
8. allow-policy-legacy
9. allow-diff
10. cargo-allow
```

Each package was dry-run verified immediately before publishing with:

```bash
cargo publish --dry-run -p <crate> --locked
```

Then published with:

```bash
cargo publish -p <crate> --locked
```

## Published Registry State

The following crates were verified visible on crates.io:

```text
allow-core 0.1.8
allow-policy 0.1.8
allow-inventory 0.1.8
allow-files 0.1.8
allow-rust 0.1.8
allow-match 0.1.8
allow-report 0.1.8
allow-policy-legacy 0.1.8
allow-diff 0.1.8
cargo-allow 0.1.8
```

Registry visibility was checked with:

```bash
cargo search cargo-allow --limit 20
cargo search allow-core --limit 20
cargo search allow-policy --limit 20
cargo search allow-report --limit 20
```

The final `cargo-allow` dry-run resolved every internal `0.1.8` crate from
crates.io before publication.

## Final Verification

After `cargo-allow` was published, the release was verified with the installed
crates.io binary:

```bash
cargo install cargo-allow --version 0.1.8 --locked --root target/cargo-allow-0.1.8-install --force
.\target\cargo-allow-0.1.8-install\bin\cargo-allow.exe --version
.\target\cargo-allow-0.1.8-install\bin\cargo-allow.exe doctor --profile spec-system --format json --output target\cargo-allow\install-0.1.8-spec-system-doctor.json
.\target\cargo-allow-0.1.8-install\bin\cargo-allow.exe check --profile spec-system --mode audit --format json --output target\cargo-allow\install-0.1.8-spec-system.json
.\target\cargo-allow-0.1.8-install\bin\cargo-allow.exe worklist --profile spec-system --format json --output target\cargo-allow\install-0.1.8-spec-system-worklist.json
.\target\cargo-allow-0.1.8-install\bin\cargo-allow.exe explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target\cargo-allow\install-0.1.8-spec-system-explain.json
.\target\cargo-allow-0.1.8-install\bin\cargo-allow.exe check --mode no-new --format markdown --receipt target\cargo-allow\install-0.1.8-check.receipt.json --output target\cargo-allow\install-0.1.8-check.md
```

The installed binary reported `cargo-allow 0.1.8`.

The installed-binary spec-system checks reported:

```text
doctor: mode = blocking, status = passed, readiness.ready = true
check: mode = blocking, status = passed, 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 work items
worklist: status = passed, 0 findings, 0 work items
explain CARGO-ALLOW-SPEC-0001: status = passed, 1 artifact, 5 links, 0 findings, 0 work items
```

The installed-binary default no-new check passed with `622` files scanned,
`115` matched findings, and `0` new findings.

## Post-Onboarding Self-Hosting Check

After the onboarding front door, adoption-friction template links, and first
external-dogfood handoff were added, the published `cargo-allow 0.1.8` binary
was run against the cargo-allow repository again.

The post-onboarding installed-binary checks reported:

```text
cargo-allow --version: cargo-allow 0.1.8
spec-system doctor: mode = blocking, status = passed, readiness.ready = true
spec-system check: mode = blocking, status = passed, 6 artifacts, 17 links, 4 support-tier rows, 0 findings, 0 work items
spec-system worklist: status = passed, 0 findings, 0 work items
spec-system explain CARGO-ALLOW-SPEC-0001: status = passed, 1 artifact, 5 links, 0 findings, 0 work items
default no-new: status = passed, 624 files scanned, 117 matched findings, 0 new findings, 0 stale receipts
```

## GitHub Release

The public GitHub Release body is recorded in
[docs/release/github/v0.1.8.md](github/v0.1.8.md). The `v0.1.8` tag and
GitHub Release were created after publication:

```text
GitHub Release: https://github.com/EffortlessMetrics/cargo-allow/releases/tag/v0.1.8
published: 2026-06-12T21:40:24Z
status: public, non-draft, non-prerelease
```

## Known Limitations

The 0.1.8 release remains source-tree/source-syntax scoped by default. The
opt-in spec-system profile is structural source-tree graph validation. It may
parse TOML and Markdown and inspect repository files. It may verify IDs, paths,
statuses, links, support-tier proof fields, active-goal references, and closeout
links.

It must not execute proof commands, call GitHub APIs, inspect external PR state,
run Cargo, rustc, Clippy, build scripts, proc macros, ripr, unsafe-review,
coverage, or network checks as part of the cargo-allow scan.

## Rollback

Crates.io versions are immutable. If publication is wrong:

```bash
cargo yank <crate> --version 0.1.8
```

Then prepare and publish a fixed patch release. Do not reuse `0.1.8` for
corrected contents.
