# 0.1.11 ReleaseQualificationV1

Assessed against current `main` at `b2958b01783f58cdf976526826c764771d7c006f`
on 2026-07-15. The candidate version remains provisional; the workspace is
currently `0.1.10`.

## Decision

```text
candidate_ready = false
tag_ready = false
publication_performed = false
```

The supported exception-ledger core is substantially complete, but the next
release cannot yet claim package/install readiness, Rust 1.85 coverage on every
claimed platform, or authentication/provenance readiness.

## Capability matrix

| Capability | Status | Evidence | Remaining boundary |
| --- | --- | --- | --- |
| Base exception ledger | Supported | merged core ledger slices and current no-new proof | source-tree-only scanner claim |
| No-new ratchet and diff | Supported | current main no-new receipt; hosted CI | no semantic or coverage claim |
| Checked mutation lifecycle | Supported | mutation receipts, lifecycle corpus, change-control dogfood | fixture-backed supported journey |
| List/explain/worklist/read model | Supported | converged lifecycle/read-model PRs and post-merge spec audit | no global work authority |
| Issue-first manage-an-exception journey | Supported | #2310 operator guide and GOAL-0004 closeout | docs do not automate GitHub |
| Fast/full authoritative proof | Supported | #2316 merged; hosted fast/contract sentinels and full workspace union pass | local Windows contract run timed out and remains instrument-limited |
| Package/install/rollback basics | NotProven | #2256 remains open | candidate package and clean-install smoke required |
| Release authentication/provenance | BlockerBeforeTag | #2115 and #2047 remain open | Trusted Publishing and signed manifest require current receipts |

Optional spec-led, RIPR, focused-proof, and Codex-pack rows are `NotIncluded`
for this qualification until #2208 ratifies a retained MVP and their individual
gates pass. LSP, portfolio/work-claim control, and autonomous GitHub
orchestration are not release capabilities.

## Platform and MSRV matrix

| Dimension | Status | Evidence |
| --- | --- | --- |
| Linux stable | Partial | hosted CI passed the current main proof union |
| Windows stable | NotProven | fast binary proof passed; contract integration timed out at 10 minutes while saved-artifact subprocess targets were active |
| macOS stable | NotProven | no current candidate smoke receipt |
| Rust 1.85 MSRV | NotProven | declared in `Cargo.toml`; dedicated qualification lane remains under #1901/#2262 |

No platform is promoted from Rust portability alone.

## Candidate blocker dispositions

| Issue | Disposition | Current evidence |
| --- | --- | --- |
| #1857 revision argument boundary | StaleOrAlreadyFixed | closed after the exact revision-option hardening in #2287 |
| #1840 evidence-path error classification | StaleOrAlreadyFixed | issue is closed; retain the merged proof as historical evidence |
| #1916 unbounded source reads | BlockerBeforeCandidate | open; current source still uses whole-file reads without a release qualification bound |
| #1917 recursive inventory bounds | BlockerBeforeCandidate | open; current qualification has no bounded depth/entry receipt |
| #1924 wildcard complexity | BlockerBeforeCandidate | open; current qualification has no pathological-pattern bound or receipt |
| #2115 Trusted Publishing | BlockerBeforeTag | operator configuration and current workflow receipt are absent |
| #2047 signed multi-crate manifest | BlockerBeforeTag | final tag/commit/crate/checksum/signature binding is not receipted |
| #2256 package/install smoke | BlockerBeforeCandidate | pre-publication package and clean-repository install proof is absent |
| #2261 compatibility inventory | NeedsRepositoryDecision | version and exact capability matrix remain provisional |

## Exact proof run

The following passed on the post-merge current main worktree:

```text
cargo test -p allow-policy spec_system --locked
cargo check -p cargo-allow --tests --locked
cargo run -p cargo-allow -- check --profile spec-system --mode audit --format json
cargo run -p cargo-allow -- worklist --profile spec-system --format json
cargo run -p cargo-allow -- doctor --profile spec-system --format json
cargo run -p cargo-allow -- explain CARGO-ALLOW-GOAL-0004 --profile spec-system --format json
cargo run -p cargo-allow -- check --mode no-new --format markdown
```

The spec audit passed with zero work items and one advisory missing-import-root
finding. The worklist passed with one unrelated pre-existing PLAN-0002 item.
The historical GOAL-0004 explain path passed, and `.allow/goals/active.toml`
does not exist.

## Gate reasons

`candidate_ready` becomes true only after #2262 has no
`BlockerBeforeCandidate` residue, #2261 fixes the version and capability
matrix, and #2256 proves the exact source candidate on every claimed platform.
`tag_ready` additionally requires Trusted Publishing, release controls, and
the signed multi-crate manifest from #2115/#2047.

This report authorizes no version bump, tag, registry publication, or signed
release-manifest completion.
