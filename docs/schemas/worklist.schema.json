{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://effortlessmetrics.dev/schemas/cargo-allow/worklist.v1.schema.json",
  "title": "cargo-allow worklist v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "schema_id",
    "tool",
    "command",
    "claim_boundary",
    "scanner_limitations",
    "inventory",
    "summary",
    "work_items"
  ],
  "properties": {
    "schema_version": {
      "const": 1
    },
    "schema_id": {
      "const": "cargo-allow.worklist.v1"
    },
    "tool": {
      "const": "cargo-allow"
    },
    "command": {
      "const": "worklist"
    },
    "claim_boundary": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/claim_boundary_flag"
      },
      "contains": {
        "const": "source_tree_inventory"
      }
    },
    "scanner_limitations": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/scanner_limitation"
      },
      "description": "Explicit scanner and execution limitations for this source-tree worklist artifact.",
      "contains": {
        "const": "cargo_metadata_not_invoked"
      }
    },
    "inventory": {
      "$ref": "#/$defs/inventory"
    },
    "filters": {
      "$ref": "#/$defs/filters",
      "description": "Applied worklist filters. Absent in older v1 artifacts; null fields mean no filter was applied for that dimension."
    },
    "summary": {
      "$ref": "#/$defs/summary"
    },
    "work_items": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/work_item"
      }
    }
  },
  "$defs": {
    "claim_boundary_flag": {
      "type": "string",
      "enum": [
        "source_tree_inventory",
        "source_syntax_only",
        "cargo_metadata_not_invoked",
        "cargo_commands_not_invoked",
        "rustc_not_invoked",
        "clippy_not_invoked",
        "build_scripts_not_executed",
        "proc_macros_not_executed",
        "macro_expansion_not_analyzed",
        "macro_token_tree_contents_not_analyzed",
        "type_information_not_analyzed",
        "mir_not_analyzed",
        "build_output_not_analyzed",
        "control_flow_not_analyzed",
        "data_flow_not_analyzed",
        "external_evidence_tools_not_invoked",
        "repository_code_not_executed"
      ]
    },
    "scanner_limitation": {
      "type": "string",
      "enum": [
        "cargo_metadata_not_invoked",
        "cargo_commands_not_invoked",
        "rustc_not_invoked",
        "clippy_not_invoked",
        "build_scripts_not_executed",
        "proc_macros_not_executed",
        "macro_expansion_not_analyzed",
        "macro_token_tree_contents_not_analyzed",
        "type_information_not_analyzed",
        "mir_not_analyzed",
        "build_output_not_analyzed",
        "control_flow_not_analyzed",
        "data_flow_not_analyzed",
        "external_evidence_tools_not_invoked",
        "repository_code_not_executed"
      ]
    },
    "inventory": {
      "type": "object",
      "additionalProperties": false,
      "required": ["scope", "scanner", "source"],
      "properties": {
        "scope": {
          "const": "source_tree"
        },
        "scanner": {
          "const": "source_syntax"
        },
        "source": {
          "enum": [
            "unknown",
            "git_tracked",
            "filesystem_fallback",
            "filesystem_include_untracked"
          ]
        },
        "root": {
          "type": "string",
          "minLength": 1,
          "description": "Local source-tree root used for this worklist. This may be an absolute path in local artifacts."
        },
        "files_scanned": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of source-tree inventory files considered before scanner filtering, when a full inventory was collected."
        }
      }
    },
    "filters": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "kind": {
          "type": ["string", "null"],
          "description": "Applied --kind filter, or null when the worklist includes all governed kinds."
        },
        "family": {
          "type": ["string", "null"],
          "description": "Applied --family filter, or null when all scanner or policy families are included."
        },
        "item_kind": {
          "type": ["string", "null"],
          "description": "Applied --item-kind filter, or null when all work item queue kinds are included."
        },
        "status": {
          "type": ["string", "null"],
          "enum": [
            "matched",
            "new",
            "stale",
            "expired",
            "review_due",
            "ambiguous",
            "invalid_selector",
            "missing_required_field",
            "evidence_missing",
            "baseline_debt",
            null
          ],
          "description": "Applied --status filter, or null when all match statuses are included."
        },
        "allow_id": {
          "type": ["string", "null"],
          "description": "Applied --allow-id filter, or null when all allow entries are included."
        },
        "path": {
          "type": ["string", "null"],
          "description": "Applied --path source-tree path or path-prefix filter, or null when all work item paths are included."
        },
        "source_package": {
          "type": ["string", "null"],
          "description": "Applied --source-package filter, or null when all scanner-provided source package contexts are included."
        },
        "owner": {
          "type": ["string", "null"],
          "description": "Applied --owner filter, or null when all policy owners are included."
        },
        "classification": {
          "type": ["string", "null"],
          "description": "Applied --classification filter, or null when all policy classifications are included."
        },
        "baseline_debt": {
          "type": "boolean",
          "description": "Applied --baseline-debt filter. True means only generated baseline debt work items are included."
        },
        "broad_scope": {
          "type": "boolean",
          "description": "Applied --broad-scope filter. True means only broad source-tree scope advisory work items are included."
        },
        "risk": {
          "type": ["string", "null"],
          "enum": ["low", "medium", "high", null],
          "description": "Applied --risk filter, or null when all risk levels are included."
        },
        "difficulty": {
          "type": ["string", "null"],
          "enum": ["small", "medium", null],
          "description": "Applied --difficulty filter, or null when all difficulty levels are included."
        },
        "missing_evidence": {
          "type": "boolean",
          "description": "Applied --missing-evidence filter. True means only policy-backed work items with no evidence references are included."
        }
      }
    },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "work_items",
        "high",
        "medium",
        "low",
        "small_difficulty",
        "medium_difficulty"
      ],
      "properties": {
        "work_items": {
          "type": "integer",
          "minimum": 0
        },
        "high": {
          "type": "integer",
          "minimum": 0
        },
        "medium": {
          "type": "integer",
          "minimum": 0
        },
        "low": {
          "type": "integer",
          "minimum": 0
        },
        "small_difficulty": {
          "type": "integer",
          "minimum": 0,
          "description": "Count of work items estimated as small difficulty."
        },
        "medium_difficulty": {
          "type": "integer",
          "minimum": 0,
          "description": "Count of work items estimated as medium difficulty."
        }
      }
    },
    "work_item": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "kind",
        "risk",
        "difficulty",
        "status",
        "allow_id",
        "finding_index",
        "path",
        "source_package",
        "message",
        "suggested_actions",
        "proof_commands"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^work-[a-z0-9-]+-[0-9]{4}$"
        },
        "kind": {
          "enum": [
            "new_unreceipted_finding",
            "occurrence_limit_exceeded",
            "expired_allow",
            "stale_allow",
            "ambiguous_selector",
            "unsafe_missing_evidence",
            "missing_evidence",
            "missing_required_field",
            "invalid_selector",
            "baseline_debt",
            "review_due",
            "matched",
            "broad_scope",
            "broken_evidence_link",
            "weak_evidence_reference"
          ],
          "description": "Stable queue item kind emitted by cargo-allow worklist."
        },
        "exception_kind": {
          "type": ["string", "null"],
          "enum": [
            "panic",
            "unsafe",
            "lint_exception",
            "non_rust_file",
            "generated_code",
            "policy_exception",
            null
          ],
          "description": "Governed source exception surface behind the work item, when known."
        },
        "family": {
          "type": ["string", "null"],
          "description": "More specific scanner or policy family behind the work item, when known."
        },
        "owner": {
          "type": ["string", "null"],
          "description": "Policy owner for the related allow entry, when the work item is tied to policy."
        },
        "classification": {
          "type": ["string", "null"],
          "description": "Policy classification for the related allow entry, when available."
        },
        "reason": {
          "type": ["string", "null"],
          "description": "Policy reason for the related allow entry, when available."
        },
        "created": {
          "type": ["string", "null"],
          "description": "Policy creation date for the related allow entry, when available."
        },
        "review_after": {
          "type": ["string", "null"],
          "description": "Policy review-after date for the related allow entry, when available."
        },
        "expires": {
          "type": ["string", "null"],
          "description": "Policy expiry date for the related allow entry, when available."
        },
        "evidence_count": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "Number of evidence references on the related allow entry, when available."
        },
        "selector_precision": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "Selector precision score for the related allow entry, when available. Higher scores indicate narrower policy selectors; this is routing metadata, not proof of correctness."
        },
        "risk": {
          "enum": ["high", "medium", "low"]
        },
        "difficulty": {
          "enum": ["small", "medium"]
        },
        "status": {
          "enum": [
            "matched",
            "new",
            "stale",
            "expired",
            "review_due",
            "ambiguous",
            "invalid_selector",
            "missing_required_field",
            "evidence_missing",
            "baseline_debt"
          ]
        },
        "allow_id": {
          "type": ["string", "null"]
        },
        "finding_index": {
          "type": ["integer", "null"],
          "minimum": 0
        },
        "path": {
          "type": ["string", "null"],
          "description": "Source-tree path for source-backed work items, or a local evidence path for broken_evidence_link items. Null means the item is not tied to a source-tree path, including weak_evidence_reference items whose evidence target is not locally validated."
        },
        "evidence_reference": {
          "$ref": "#/$defs/evidence_reference",
          "description": "Structured evidence diagnostic for broken_evidence_link and weak_evidence_reference work items. Absent for work items that are not tied to a specific evidence reference."
        },
        "source_package": {
          "type": ["string", "null"],
          "description": "Explicit source-tree package context when provided by a scanner; not Cargo metadata or build proof."
        },
        "message": {
          "type": "string",
          "minLength": 1
        },
        "suggested_actions": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "proof_commands": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^cargo-allow ",
            "description": "Suggested follow-up command. cargo-allow emits standalone cargo-allow commands here; it does not execute them."
          }
        }
      }
    },
    "evidence_reference": {
      "type": "object",
      "additionalProperties": false,
      "required": ["raw", "prefix", "target", "status", "message"],
      "properties": {
        "raw": {
          "type": "string",
          "minLength": 1,
          "description": "Original evidence reference string from the allow entry."
        },
        "prefix": {
          "type": ["string", "null"],
          "description": "Evidence reference prefix before ':', or null for unstructured evidence strings."
        },
        "target": {
          "type": ["string", "null"],
          "description": "Normalized evidence reference target when present. This is evidence metadata, not the work item source-tree path."
        },
        "status": {
          "$ref": "#/$defs/evidence_reference_status"
        },
        "category": {
          "$ref": "#/$defs/evidence_reference_category",
          "description": "Optional human-facing diagnostic category. Current producers emit it; it remains optional for v1 compatibility with older artifacts."
        },
        "message": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "evidence_reference_category": {
      "type": "string",
      "enum": [
        "present",
        "missing",
        "invalid_local_path",
        "not_local",
        "unknown_prefix",
        "untyped"
      ],
      "description": "Human-facing diagnostic category for evidence references. This preserves the stable status field while distinguishing weak evidence causes such as unknown prefixes and untyped strings."
    },
    "evidence_reference_status": {
      "type": "string",
      "enum": [
        "local_file_present",
        "local_file_missing",
        "invalid_local_path",
        "traceability_only",
        "unstructured"
      ]
    }
  }
}
