# repo-edit parity fixtures (#2602-A / #2602-B)

Contract-only fixtures for mutation lock convergence, path containment,
single-target atomic write/replace, generic apply receipts, and init command
migration. Runtime parity replay lands with remaining cargo-allow command
migration packets.
