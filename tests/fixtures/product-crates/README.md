# Product crate architecture fixtures (#2580)

Synthetic `cargo metadata` JSON fixtures for dependency-law validation. Each file
is a minimal metadata document with only the `packages` array required by the
validator.
