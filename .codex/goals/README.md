# Active Goals (legacy path)

**This path is no longer canonical.** Active goal manifests live under
[`.allow/goals/`](../.allow/goals/README.md) as of C4 dogfood migration
(CARGO-ALLOW-PLAN-0004 C4 / CARGO-ALLOW-CLOSEOUT-0008).

See [`.allow/goals/active.toml`](../.allow/goals/active.toml) for current
execution state. Archived goal manifests moved to `.allow/goals/archive/`.

## Claim Boundary

The opt-in `spec-system` profile reads the configured goals root from
`.allow/profiles/spec-system.toml`. This legacy `.codex/goals/` directory is
retained only as a migration pointer.
