kind: Fixed
body: >-
  Mutation: `add` now reuses the policy selected for the invocation when
  scanning and recording an added exception, avoiding a second configuration
  discovery that could produce mixed policy identity.
