kind: Fixed
body: >-
  Round-trip authored Rust target identities through intent-compiler by
  accepting the canonical integration_test: target form, retaining the test:
  compatibility alias, and resolving legacy self-hosted registrations that
  keep only a target name against the supplied exact inventory.
