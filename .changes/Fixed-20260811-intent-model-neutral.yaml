kind: Fixed
body: >-
  Remove intent-model's normal dependency on cargo-allow product types while
  preserving authored parse classifications, bounded source reads, normalized
  paths, and stable content identities inside the intent package boundary.
  The generated movement authority now receipts the new neutral error source.
