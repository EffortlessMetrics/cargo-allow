kind: Fixed
body: >-
  Preserve `--allow-id` and `--include-untracked` in the apply command emitted
  by a prune preview so following that command keeps the reviewed selection
  and source inventory.
