schema_version = "1.0"

id = "CARGO-ALLOW-GOAL-0001"
title = "Spec-system profile"
status = "archived"
owner = "codex"
created = "2026-06-12"

objective = """
Build an opt-in spec-system profile into cargo-allow so repositories can govern
their proposal, spec, ADR, implementation-plan, active-goal, support-tier,
policy-ledger, and closeout graph without changing default source-exception
behavior.
"""

linked_proposal = "CARGO-ALLOW-PROP-0001"
linked_spec = "CARGO-ALLOW-SPEC-0001"
linked_support_tier = "CARGO-ALLOW-SUPPORT-0001"
linked_plan = "plans/spec-system/implementation-plan.md"
linked_plan_status = "done"

claim_boundary = """
This manifest is agent execution state. It does not implement spec-system
linting, prove claims, execute proof commands, or replace the proposal, spec,
support-tier map, implementation plan, policy ledgers, or closeouts.
"""

[[work_item]]
id = "spec-system-pr-033"
status = "done"
title = "Finalize spec-system profile closeout"
linked_proposal = "CARGO-ALLOW-PROP-0001"
linked_spec = "CARGO-ALLOW-SPEC-0001"
linked_plan = "plans/spec-system/implementation-plan.md"
linked_closeout = "CARGO-ALLOW-CLOSEOUT-0001"
proof_commands = [
  "cargo run -p cargo-allow -- doctor --profile spec-system --format json",
  "cargo run -p cargo-allow -- check --profile spec-system --mode audit --format json --output target/cargo-allow/spec-system.json",
  "cargo run -p cargo-allow -- worklist --profile spec-system --format json --output target/cargo-allow/spec-system-worklist.json",
  "cargo run -p cargo-allow -- explain CARGO-ALLOW-SPEC-0001 --profile spec-system --format json --output target/cargo-allow/spec-system-explain.json",
  "git diff --cached --check",
  "cargo fmt --all --check",
  "cargo clippy --workspace --all-targets -- -D warnings",
  "cargo test --workspace",
  "cargo run -p cargo-allow -- check --mode no-new --format markdown --receipt target/cargo-allow/check.receipt.json --output target/cargo-allow/check.md",
]
evidence_notes = [
  "The opt-in spec-system profile is implemented as one governance profile alongside the default source-exception ledger.",
  "The final closeout records structural profile behavior, dogfood evidence, worklist/readiness/explain surfaces, and no-execution claim boundaries.",
  "Keep CARGO-ALLOW-SUPPORT-0001 advisory unless a separate support-tier promotion decision is made.",
  "Do not package, tag, publish, or claim stable support without explicit release authorization.",
]
