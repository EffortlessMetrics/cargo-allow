# PR #4153: receipt retained independent source-review evidence and this note.
# These exact file exceptions do not authorize merge, CI bypass, or release.

schema_version = 1

[[records]]
allow_ids = ["allow-0921"]
change_kinds = ["added_allow"]
reason = "Retain the exact PR #4153 structured source-review disposition under the current-head review protocol; the coordinator still verifies current-pair CI and merge readiness."

[[records]]
allow_ids = ["allow-0922"]
change_kinds = ["added_allow"]
reason = "Receipt this authored revision note for the two exact retained-review evidence files in PR #4153; no broader review-disposition or revision-directory exception is granted."
