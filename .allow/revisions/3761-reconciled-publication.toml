schema_version = 1
# Revision note for #3761 slice 2: manifest rows from classified publications.

# allow-0697..allow-0699 receipt the test-only fail-closed expects in the
# reconciled-publication construction tests; expect on constructed fixtures
# is the established test idiom and assert/assert_eq findings are covered by
# the retained test-assert entries. No existing exception is weakened, no
# expiry is extended, and no scope is broadened: every record below is an
# addition. policy/allow.toml keeps its CRLF line-ending contract; the net
# transition is append-only.


[[records]]
allow_ids = ["allow-0697"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:ba70a5304e9c5cb8919ec5c50909450fc3077d26f213464f1b27088245634f94"
reason = "Test-only expect in the reconciled-publication manifest-row construction test (#3761); expect on a constructed fixture is the established test idiom."

[[records]]
allow_ids = ["allow-0698"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:86e5470063544e6597ee6e17c0ef3beeb58f7bca38433aad9bf7d563a363b005"
reason = "Test-only expect on the classified batch construction (#3761); test-only fail-closed helper."

[[records]]
allow_ids = ["allow-0699"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:81ceaeebaf9f66ddeab831ab1de736819f8a9a06182f78e4bfefeecdd44caee9"
reason = "Test-only expect in the stale-row construction test (#3761); test-only fail-closed helper."
