schema_version = 1
# Revision note for the #3963 Linux cache experiment.

# This diff adds the typed Linux cache experiment contract:
# CiCacheExperimentV1 / CiCacheLaneObservationV1 / CiCacheParityRowV1 /
# CiCacheVerdictV1 in allow-report, the retained hosted-evidence
# receipt docs/ci/receipts/ci-cache-experiment-v1.json (the compiled
# lane retains one trusted warm full-match main run; the window's
# further warm runs and the untrusted restore-only pull request are
# carried as named window facts in the limits), and the law tests
# binding the #3900 cache action to the trust,
# compatibility, parity, and measurement laws. Verdict law: Accepted
# requires cold/warm/disabled/fallback coverage, a two-warm
# distribution per lane, equal semantic results across compared
# postures, a falsified untrusted no-save boundary, and a pinned
# action everywhere; parity divergence or an incident is Rejected;
# everything short of the evidence set is NeedsMoreData with the
# exact missing rows and rollback route, which is the retained
# receipt's honest verdict. The source-exception additions receipt
# only the syntax-visible constructs the new surfaces introduce:
# allow-11229/allow-11230 are glob-ratcheted expect baselines for the
# law tests, allow-11231 receipts the retained receipt file, and
# allow-11232 receipts this revision-note file itself per the per-note
# receipt law. No existing exception is weakened, no expiry is
# extended, and no scope is broadened: the records below are
# additions. policy/allow.toml keeps its CRLF line-ending contract;
# the net transition is append-only.

[[records]]
allow_ids = ["allow-11229"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:6c52aa86291d3d83960543d6de655b03ebd7201ca40bd178a2c5a9c7ebdf8cfd"

[[records]]
allow_ids = ["allow-11230"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:2e948a668e39c2f18c5de2836ea1b2da0aa3ef173843aab5b00abe230e39370a"

[[records]]
allow_ids = ["allow-11231"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:ee7a5a91e4c5231cfffda2331e8b825bd74d107fa14bc0ddbbbca013572330b1"

[[records]]
allow_ids = ["allow-11232"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:effb2e825263d41296901be657bbe668bc8b7b3bb6692ba239b95e968b89b1ce"

[[records]]
allow_ids = ["allow-11233"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:4af63ca5bb7b48a06661a9fa59d5cf6ec886b9bfc183bb6716ac39b6589f4092"
