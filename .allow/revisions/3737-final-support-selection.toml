schema_version = 1
# Revision note for the #3737 final support-selection freeze slice.

# This diff adds the [final_selection] section to docs/support-matrix.toml
# (the 0.2.0 release proof denominator: thirteen typed rows with closed
# dispositions, proof owners, evidence references, claim effects, and
# staleness inputs, keyed to the typed release identity by two recomputed
# digests), the final_support_selection_v1 typed model in allow-report (the
# single implementation of the row model, identity digest role
# final-selection, and canonical selection digest), fail-closed drift tests
# in the support_matrix contract test, and the changie fragment. The
# source-exception additions receipt only the syntax-visible panicking
# constructs the new model file introduces: allow-0858 is a
# glob-ratcheted expect baseline (occurrence_limit=7) for the test module's
# fixture/invariant-guarded expects — the limit was refreshed from 6 to 7
# in-lane when the codecov closure work added negative-control tests; the
# one indexing site was rewritten as a first_mut().expect so no indexing
# receipt is needed. allow-0857 is the ledger-change-note receipt for this
# revision-note file itself, matching the per-note receipt law used for
# prior revision notes. (Ids were re-allocated from allow-0854/0855 during
# the rebase onto main c5b56860: upstream #4126 consumed those ids first,
# and the diff gate correctly rejected the collision as a
# governed-identity change.) No existing exception is weakened, no expiry
# is extended, and no scope is broadened: the records below are additions.
# policy/allow.toml keeps its CRLF line-ending contract; the net
# transition is append-only.

[[records]]
allow_ids = ["allow-0857"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:3cf30e981ca6c8a711e87ac553e930377dcdf0e6042793c115a24d9b86f06f03"
reason = "Ledger-change-note receipt for this revision-note file itself (#3737); the note authorizes the expect baseline for the final-support-selection model's test module and is receipted per the per-note receipt law."

[[records]]
allow_ids = ["allow-0858"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:4cda4102d9ba480520f8d2dd2480f6345686f08638e09d31d679646be2e56a7e"
reason = "Glob-ratcheted expect baseline for the final-support-selection model's test module (#3737); every expect guards an invariant established by immediately preceding fixture construction or typed validation in the same function, and the selection digests are recomputed from committed policy data only."
