# Revision note for #3752 slice A: checked release-identity consumer denominator.

# This diff adds two source-exception entries and four test-guard panic
# receipts, all in service of one checked denominator of release-identity
# consumers. allow-11198 receipts the new machine-checkable inventory
# policy/release-identity-consumers.toml (31 surveyed consumer rows with
# dispositions, marker classes, verified site anchors, and exact counts for
# the release-critical surfaces). allow-11199 is the self-receipt for this
# revision note file, following the established ledger_change_note precedent.
# allow-0663..allow-0666 receipt the four `panic!` invocations in the
# fail-closed helpers of release_identity_denominator_tests.rs; panic on
# unread or unscannable release surfaces is the established guard idiom in
# release_prep_tests, and assert/assert_eq findings are already covered by
# the retained test-assert entries. No existing exception is weakened, no
# expiry is extended, and no scope is broadened: every record below is an
# addition that makes release-identity parsing more visible, not less.

[[records]]
allow_ids = ["allow-11198"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:b7fbd671aab2a39d4ea8b683f7a768ab5d6ade5fb68070f0c81070bb3394c86b"
reason = "Adds the checked denominator of release-identity consumers for #3752 slice A: the inventory declares every non-Rust surface that parses, infers, or projects release-version semantics, with a #3752 disposition per row, and is enforced by release_identity_denominator_tests (undeclared constructs, row drift, exact-count drift, and -rc channel inference all fail closed)."

[[records]]
allow_ids = ["allow-11199"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:1f39ff53b2433830036dca965f0dd5119a48b785fc1d6aeb95b10d683b4bca2e"
reason = "Self-receipt for this revision note file, matching the ledger_change_note precedent: the note is a tracked non-Rust file and its own ledger entry is part of the authorized transition."
