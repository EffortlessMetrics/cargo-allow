schema_version = 1
# Revision note for the #3831 candidate-preparation plan slice.

# This diff adds the typed candidate-preparation model (allow-report), the
# hidden read-only `prep-candidate plan` command, its 12 negative controls
# plus acceptance tests, the candidate-preparation-plan-v1 schema, and the
# changie fragment. The source-exception additions receipt only the
# syntax-visible panicking constructs the new files introduce:
# allow-0712/allow-0713 are glob-ratcheted expect baselines for the model's
# fixture/invariant-guarded expects and the test module's negative-control
# expects; allow-0714..allow-0719 receipt the per-occurrence panic constructs
# in the model, the tests, and the command's bounded manifest parsers;
# allow-0720 receipts the schema contract document. No existing exception is
# weakened, no expiry is extended, and no scope is broadened: every record
# below is an addition. policy/allow.toml keeps its CRLF line-ending
# contract; the net transition is append-only.

[[records]]
allow_ids = ["allow-0712"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:59cfdc157fed19bcf2edfa1db42c1a70758f97e6837f42b085f4885f75af7da1"
reason = "Glob-ratcheted expect baseline for the candidate-preparation plan model (#3831); every expect guards an invariant established by immediately preceding validation (single source line, non-empty collections, canonical serialization) under fully controlled fixture inputs."

[[records]]
allow_ids = ["allow-0713"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:f074033d8a31cd083ae75b3fa0d8ec6af7d18277a9b065eee4a413abc4caca42"
reason = "Glob-ratcheted expect baseline for the negative-control and acceptance test module (#3831); expects run against fixtures and the read-only live projection, never against mutable state."

[[records]]
allow_ids = ["allow-0714"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:e11760bc2b00f53361bbd894429e4cb8ae64e7ff5b83603e8e6fda8320066572"
reason = "Canonicalization failure guard in the input-identity digest (#3831); serde JSON serialization of this typed identity struct is total, so the panic arm is unreachable."

[[records]]
allow_ids = ["allow-0715"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:59cbaa1f2ea1111adce25ebcaae4e4f4c325aaf6a8d6c838845e2d8a40aba1d2"
reason = "Workspace-root discovery failure fires only when the test process starts outside the repository (#3831)."

[[records]]
allow_ids = ["allow-0716"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:7749356878161174859b01c168ffa9affec758172814e2cc2a9be6192c26bd3e"
reason = "Corpus-path containment guard in the read-only gather path (#3831); paths are collected under the repository root so the strip_prefix cannot fail."

[[records]]
allow_ids = ["allow-0717"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:6677dd4e59676ebcd9b2c2ebf19aea30c5050aab7d0f4a9c521bcba000fbbc01"
reason = "ASCII-boundary slicing after a literal find anchor in the workspace-members parser (#3831); offsets come from find on ASCII markers so slice boundaries are char-safe."

[[records]]
allow_ids = ["allow-0718"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:09e344b6ed67318782a122dfb9b1ec0078ac2d48cd467719f9c029ca2f0688aa"
reason = "Same ASCII-boundary slicing law for the second members-array anchor (#3831)."

[[records]]
allow_ids = ["allow-0719"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:70b2b701fd71ddd7affb6254c006819652f27591bebf718114b700ff412e389f"
reason = "Same ASCII-boundary slicing law for the workspace-dependencies requirement parser (#3831)."

[[records]]
allow_ids = ["allow-0720"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:06f57d11558b1539b9dce7df738a6bba338001f0da6508a96c339ebac4189da8"
reason = "Receipt for the candidate-preparation-plan-v1 schema contract document (#3831); machine-readable contract for the read-only prep-candidate projection."

[[records]]
allow_ids = ["allow-0721"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:18c667050f1ada03ef415bccc8356793f2e4433133a988be54c8ce7e11a11e58"
reason = "Self-receipt for this revision note record (#3831); the note documents the added_allow transition for the candidate-preparation receipt set."

[[records]]
allow_ids = ["allow-0722"]
change_kinds = ["added_allow"]
after_fingerprint = "sha256:v1:de26d37cbe928a2f2988144d3bab01615026d6c9aa7a5601e925578d75b7852b"
reason = "Invariant-guarded product-row indexing in the closure projection (#3831); the preceding empty-closure conflict check guarantees product_rows is non-empty before the first row is read."
