name: Temporary clippy minimal graph

on:
  push:
    branches:
      - agent/spec-system-minimal-graph

permissions:
  contents: write

jobs:
  clippy:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: agent/spec-system-minimal-graph
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - name: Apply Clippy fixes and capture diagnostics
        shell: bash
        run: |
          set +e
          cargo clippy --fix --workspace --all-targets --allow-dirty --allow-staged -- -D warnings 2>&1 | tee clippy-minimal-graph.log
          status=${PIPESTATUS[0]}
          cargo fmt --all
          echo "CLIPPY_STATUS=$status" >> "$GITHUB_ENV"
          exit 0
      - name: Commit automatic fixes
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add crates/allow-policy/src/spec_system
          git commit -m "fix(spec-system): apply clippy suggestions"
          git push origin HEAD:agent/spec-system-minimal-graph
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: clippy-minimal-graph
          path: clippy-minimal-graph.log
      - name: Preserve Clippy result
        shell: bash
        run: exit "$CLIPPY_STATUS"
