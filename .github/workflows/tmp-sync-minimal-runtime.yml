name: Temporary sync minimal runtime model

on:
  push:
    branches:
      - agent/spec-system-minimal-runtime-promotion

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: agent/spec-system-minimal-runtime-promotion
          fetch-depth: 0
      - name: Merge current main
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch origin main
          git merge --no-edit origin/main
          git push origin HEAD:agent/spec-system-minimal-runtime-promotion
